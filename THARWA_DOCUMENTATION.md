# ثروة — توثيق شامل كامل
> آخر تحديث: 2026-07-04 | الإصدار الحالي: master

---

## فهرس المحتويات

1. [نظرة عامة على التطبيق](#1-نظرة-عامة)
2. [البنية التقنية](#2-البنية-التقنية)
3. [قاعدة البيانات — الجداول الكاملة](#3-قاعدة-البيانات)
4. [localStorage — المفاتيح الكاملة](#4-localstorage)
5. [تسجيل الدخول والمصادقة](#5-تسجيل-الدخول)
6. [لوحة التحكم — dashboard](#6-لوحة-التحكم)
7. [سجل المعاملات — transactions](#7-سجل-المعاملات)
8. [الأرباح الموزعة — dividends](#8-الأرباح-الموزعة)
9. [التدفقات النقدية — cashflows](#9-التدفقات-النقدية)
10. [قاعدة بيانات أسهمي — userdb](#10-قاعدة-بيانات-أسهمي)
11. [أهداف الأسهم والقطاعات — targets](#11-أهداف-الأسهم-والقطاعات)
12. [أسهم تحت المراقبة — watchlist](#12-أسهم-تحت-المراقبة)
13. [الرؤية المستقبلية — forecast](#13-الرؤية-المستقبلية)
14. [الأداء التاريخي — performance](#14-الأداء-التاريخي)
15. [مهام المحفظة — tasks](#15-مهام-المحفظة)
16. [دفتر المراجعة — review-log](#16-دفتر-المراجعة)
17. [صافي الثروة — networth](#17-صافي-الثروة)
18. [مقسّم الراتب — salary](#18-مقسم-الراتب)
19. [العقارات — realestate](#19-العقارات)
20. [الصكوك — sukuk](#20-الصكوك)
21. [أهداف الحياة — life-goals](#21-أهداف-الحياة)
22. [مخزون المنزل — inventory](#22-مخزون-المنزل)
23. [المتابعة المدرسية — school](#23-المتابعة-المدرسية)
24. [متابعة كندة — school-kanda](#24-متابعة-كندة)
25. [مزامنة المعاملات — reconcile](#25-مزامنة-المعاملات)
26. [أدوات التحليل والحسابات](#26-أدوات-التحليل)
27. [الإعدادات — settings](#27-الإعدادات)
28. [لوحة الإدارة — admin](#28-لوحة-الإدارة)
29. [محرّك القرار — decision-engine](#29-محرك-القرار)
30. [جني الأرباح — profit-taking](#30-جني-الأرباح)
31. [الخوارزميات الرئيسية](#31-الخوارزميات-الرئيسية)
32. [النسخ الاحتياطي والاستعادة](#32-النسخ-الاحتياطي)
33. [utils.js — الوظائف المشتركة](#33-utils)
34. [الأمان والبنية الدفاعية](#34-الأمان)
35. [Edge Function — تحديث الأسعار](#35-edge-function)
36. [إضافات يوليو 2026 — تحليل المخاطر وإجراءات الشركات](#36-إضافات-يوليو-2026)

---

## 1. نظرة عامة

**ثروة** هو تطبيق ويب شخصي لتتبع وتحليل المحفظة الاستثمارية وإدارة الثروة الكاملة. مبني بـ HTML/CSS/JavaScript خالص (بدون فريمووركات) مع Supabase كقاعدة بيانات وخدمة مصادقة.

### الهدف
أداة حسابية شخصية — لا تقدم توصيات استثمارية ولا خدمات مالية. كل المدخلات يدوية من المستخدم.

### الصفحات الكاملة (31 صفحة)

| الملف | الوظيفة | التخزين |
|---|---|---|
| `index.html` | تسجيل الدخول / إنشاء حساب | Supabase Auth |
| `dashboard.html` | لوحة التحكم الرئيسية | Supabase |
| `transactions.html` | سجل المعاملات (شراء/بيع/منح) | Supabase |
| `dividends.html` | الأرباح الموزعة | Supabase |
| `cashflows.html` | التدفقات النقدية (إيداع/سحب) | Supabase |
| `userdb.html` | قاعدة بيانات أسهمي الشخصية | Supabase |
| `targets.html` | أهداف الأوزان وإعادة التوازن | Supabase |
| `watchlist.html` | أسهم تحت المراقبة + تحليل أثر التنويع | Supabase |
| `forecast.html` | الرؤية المستقبلية (3 سيناريوهات معايَرة على تاسي) | Supabase + localStorage |
| `performance.html` | الأداء التاريخي + مقارنة تاسي + السلوك الاستثماري | Supabase + localStorage |
| `tasks.html` | مهام المحفظة | Supabase |
| `review-log.html` | دفتر المراجعة (مع مرفقات) | Supabase |
| `networth.html` | صافي الثروة | Supabase |
| `salary.html` | مقسّم الراتب | Supabase + localStorage |
| `realestate.html` | العقارات | Supabase |
| `sukuk.html` | الصكوك والاستثمارات الثابتة | Supabase + localStorage |
| `life-goals.html` | أهداف الحياة | localStorage |
| `inventory.html` | مخزون المنزل | localStorage |
| `school.html` | المتابعة المدرسية (متعدد أبناء) | Supabase + localStorage |
| `school-kanda.html` | متابعة مخصصة لكندة | Supabase + localStorage |
| `reconcile.html` | مزامنة مع كشف الوسيط | — (عرض فقط) |
| `avg-calculator.html` | حاسبة متوسط سعر الشراء | — (لا تخزين) |
| `stock-valuation.html` | تقدير القيمة العادلة للأسهم (7 نماذج + سجل دائم) | Supabase (`user_settings`) |
| `portfolio-rating.html` | تقييم أمان المحفظة (0–10) | localStorage (لقطة) |
| `emergency-fund.html` | حاسبة صندوق الطوارئ | — (لا تخزين) |
| `invest-tips.html` | نصائح المستثمر | — (محتوى ثابت) |
| `profit-taking.html` | مؤشر جني الأرباح السلوكي/الكمي (0–100) | — (لا تخزين) |
| `decision-engine.html` | محرّك قرار آلي يطبّق دستور CLAUDE.md | Supabase + `user_settings` |
| `settings.html` | الإعدادات والنسخ الاحتياطي | Supabase + localStorage |
| `admin.html` | لوحة الإدارة (للمدير فقط) | Supabase (admin tables) |
| `maintenance.html` | صفحة الصيانة | — (عرض فقط) |

> **ملاحظة تخزين:** بعض الصفحات المصنّفة "localStorage" فقط في الإصدار السابق (الراتب، الصكوك، المتابعة المدرسية) أصبحت تُزامَن الآن عبر جدول `user_settings` في Supabase — localStorage صار مجرد **cache محلي**، ومصدر الحقيقة هو Supabase.

---

## 2. البنية التقنية

```
Frontend:  HTML5 + CSS3 + JavaScript (ES2020) — بدون فريمووركات
Backend:   Supabase (PostgreSQL + Auth + Edge Functions + RLS)
Charts:    Chart.js v4.4
Excel:     SheetJS (xlsx v0.18.5)
Storage:   Supabase (بيانات محمية + user_settings كمصدر حقيقة للمزامنة) + localStorage (cache محلي)
Auth:      Supabase Auth (email/password)
Deploy:    GitHub Pages / أي استضافة ثابتة
Dev:       Node.js server.js (منفذ 8080)
```

### ملفات JS المشتركة (تُحمَّل في كل صفحة)

| الملف | الحجم | المحتوى |
|---|---|---|
| `js/supabase.js` | صغير | تهيئة Supabase client (URL + anon key) |
| `js/auth.js` | صغير | `requireAuth()`, `logout()`, فحص الصيانة |
| `js/utils.js` | 40 KB | دوال مشتركة: تنسيق، XIRR، تنويع (HHI)، شرح البطاقات، theme، nav، inline editing، toast |

### ملفات JS الخاصة بكل صفحة (بعد نمو كبير في عدة ملفات)

| الملف | الحجم | ملاحظة النمو |
|---|---|---|
| `js/dashboard.js` | 243 KB | +80 KB — بطاقات تنويع/صحة/تعادل/FIRE جديدة |
| `js/settings.js` | 134 KB | +48 KB — جدول 17/محرك القرار/TWR في التقرير |
| `js/performance.js` | 96 KB | +36 KB — تبويبا مؤشرات التوزيع والسلوك الاستثماري |
| `js/dividends.js` | 91 KB | +16 KB — مؤشر ثقة موسّع + توحيد Forward |
| `js/forecast.js` | 89 KB | +30 KB — DCA متعدد الفترات + معايرة تاسي |
| `js/decision-engine.js` | 78 KB | **جديد بالكامل** |
| `js/targets.js` | 56 KB | +16 KB — طرق توزيع إضافية + تسجيل تقييم |
| `js/school.js` | 48 KB | +16 KB — حضور/سلوك/واجبات/اختبارات |
| `js/salary.js` | 45 KB | +7 KB — ترقية أنواع الفئات |
| `js/transactions.js` | 37 KB | +4 KB — تنبيه رمز مجهول + إعادة حساب عند تعديل التاريخ |
| `js/networth.js` | 37 KB | — |
| `js/utils.js` | 40 KB | +11 KB — computeDiversification + showCardInfo |
| `js/watchlist.js` | 27 KB | 4× — أسعار حيّة + محاكي أثر التنويع |
| `js/tickerdb.js` | 25 KB | 285 رمزاً (بدل ~40) |
| `js/admin.js` | 28 KB | إصلاحات أمان (app_metadata + مسح 16 جدولاً) |
| `js/review-log.js` | 24 KB | — |
| `js/sukuk.js` | 23 KB | — |
| `js/reconcile.js` | 17 KB | — |
| `js/life-goals.js` | 16 KB | — |
| `js/tasks.js` | 15 KB | تبسيط (لا تكرار مع محرك القرار) |
| `js/school-kanda.js` | 15 KB | — |
| `js/userdb.js` | 11 KB | — |
| `js/cashflows.js` | 11 KB | — |
| `js/inventory.js` | 10 KB | — |
| `js/realestate.js` | 9 KB | — |

### Dev Server — server.js

خادم Node.js للتطوير المحلي:
- المنفذ: 8080
- حماية path traversal: يرفض أي مسار يخرج عن جذر المشروع
- MIME types: html, css, js, json, png, jpg, svg
- رؤوس أمان: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`

---

## 3. قاعدة البيانات

### أ) جداول المستخدمين (17 جدول — محمية بـ RLS)

كل جدول يحتوي على `user_id UUID FK → auth.users` ومحمي بـ:
```sql
USING (auth.uid() = user_id)
WITH CHECK (auth.uid() = user_id)
```

#### `holdings` — المحفظة الحالية

| العمود | النوع | الوصف |
|---|---|---|
| `id` | UUID PK | معرّف تلقائي |
| `user_id` | UUID FK | مرتبط بـ auth.users |
| `ticker` | TEXT | رمز السهم (مثال: 1120) |
| `name` | TEXT | اسم السهم |
| `sector` | TEXT | القطاع |
| `shares` | NUMERIC | عدد الأسهم المتبقية |
| `avg_price` | NUMERIC | متوسط سعر الشراء (WAC) — **بدون عمولة** |
| `current_price` | NUMERIC | السعر الحالي |
| `target_weight` | NUMERIC | الوزن المستهدف % |
| `price_updated_at` | TIMESTAMPTZ | وقت آخر تحديث للسعر |

> **مهم:** `avg_price` لا يشمل العمولة — يُستخدم `transactions.total` لحسابات الربح/الخسارة الدقيقة.

#### `transactions` — سجل المعاملات

| العمود | النوع | الوصف |
|---|---|---|
| `date` | DATE | تاريخ المعاملة |
| `ticker` | TEXT | رمز السهم |
| `type` | TEXT | `buy` / `sell` / `grant` |
| `shares` | NUMERIC | عدد الأسهم |
| `price` | NUMERIC | سعر السهم |
| `commission` | NUMERIC | العمولة (حد أقصى 100 ر.س) |
| `vat` | NUMERIC | ضريبة القيمة المضافة 15% على العمولة |
| `total` | NUMERIC | شراء: `price×shares+comm+vat` / بيع: `price×shares−comm−vat` / منحة: `0` |
| `is_archived` | BOOLEAN | أرشفة ناعمة — لا حذف حقيقي |

> **AUDIT-FIX (2026-07):** تعديل حقل `date` لمعاملة موجودة يُشغّل الآن `recomputeHoldingFromTx()` تلقائياً — لأن تغيير التاريخ يُعيد ترتيب البيع بين عمليات الشراء ويُغيّر WAC. كذلك أُضيف تنبيه "رمز غير معروف" في نموذج الإضافة الفردية (كان يعمل فقط في الإضافة الجماعية سابقاً).

#### `dividends` — الأرباح الموزعة

| العمود | النوع | الوصف |
|---|---|---|
| `date` | DATE | تاريخ الاستلام |
| `ticker` | TEXT | رمز السهم |
| `amount` | NUMERIC | المبلغ الإجمالي (ليس للسهم الواحد) |
| `month` | INT | شهر التوزيع (1–12) |
| `year` | INT | سنة التوزيع |
| `is_archived` | BOOLEAN | — |

#### `cashflow_entries` — التدفقات النقدية

| العمود | النوع | الوصف |
|---|---|---|
| `date` | DATE | — |
| `type` | TEXT | `deposit` / `withdrawal` |
| `amount` | NUMERIC | المبلغ |
| `notes` | TEXT | ملاحظات |
| `is_archived` | BOOLEAN | — |

#### `net_worth_snapshots` — لقطات صافي الثروة

| العمود | النوع | الوصف |
|---|---|---|
| `date` | DATE | تاريخ اللقطة |
| `total_value` | NUMERIC | إجمالي صافي الثروة |
| `notes` | TEXT | `auto-YYYY-MM` = تلقائي من الداشبورد |
| `snapshot_json` | JSONB | تفاصيل (اختياري) |

#### `nw_assets` — أصول صافي الثروة

| العمود | النوع | الوصف |
|---|---|---|
| `category` | TEXT | `bank` / `sukuk` / `vehicle` / `other` |
| `name` | TEXT | اسم الأصل |
| `value` | NUMERIC | القيمة ر.س |
| `is_active` | BOOLEAN | أرشفة ناعمة |

#### `nw_liabilities` — التزامات صافي الثروة

| العمود | النوع | الوصف |
|---|---|---|
| `category` | TEXT | `credit_card` / `loan` / `mortgage` / `other` |
| `name` | TEXT | اسم الالتزام |
| `value` | NUMERIC | المبلغ ر.س |
| `is_active` | BOOLEAN | أرشفة ناعمة |

#### `real_estate` — العقارات

| العمود | النوع | الوصف |
|---|---|---|
| `name` | TEXT | اسم العقار |
| `type` | TEXT | نوع العقار |
| `purchase_value` | NUMERIC | تكلفة الشراء |
| `current_value` | NUMERIC | القيمة الحالية (يدوي) |
| `status` | TEXT | `owned` / `rented` / `sold` |
| `monthly_rental` | NUMERIC | الإيجار الشهري |
| `purchase_date` | DATE | تاريخ الشراء |
| `is_active` | BOOLEAN | أرشفة ناعمة |

#### `user_stocks` — قاعدة أسهمي الشخصية

| العمود | النوع | الوصف |
|---|---|---|
| `ticker` | TEXT | الرمز |
| `name` | TEXT | الاسم |
| `sector` | TEXT | القطاع |
| `in_portfolio` | BOOLEAN | هل موجود حالياً في المحفظة |

#### `stock_targets` — أهداف الأسهم

| العمود | النوع | الوصف |
|---|---|---|
| `ticker` | TEXT | — |
| `target_pct` | NUMERIC | الوزن المستهدف % |
| `entry_price` | NUMERIC | سعر منطقة الشراء (يُشتق من `portfolio_tasks.accumulate_at` إن وُجد) |
| `exit_price` | NUMERIC | سعر منطقة البيع (يُشتق من `portfolio_tasks.liquidate_above` إن وُجد) |

#### `sector_targets` — أهداف القطاعات

| العمود | النوع | الوصف |
|---|---|---|
| `sector` | TEXT | اسم القطاع |
| `target_pct` | NUMERIC | الوزن المستهدف % |

#### `watchlist` — قائمة المراقبة

| العمود | النوع | الوصف |
|---|---|---|
| `ticker` | TEXT | — |
| `name` | TEXT | — |
| `sector` | TEXT | — |
| `target_price` | NUMERIC | سعر الدخول المستهدف |
| `planned_pct` | NUMERIC | النسبة المخطط تخصيصها % |
| `notes` | TEXT | — |

#### `portfolio_cash` — نقد المحفظة

| العمود | النوع | الوصف |
|---|---|---|
| `amount` | NUMERIC | المبلغ النقدي عند الوسيط |
| `last_updated` | TIMESTAMPTZ | آخر تحديث |

#### `portfolio_tasks` — مهام المحفظة

| العمود | النوع | الوصف |
|---|---|---|
| `type` | TEXT | `liquidation`/`reduction`/`monitoring`/`accumulation`/`hold` |
| `ticker` | TEXT | رمز السهم (اختياري) |
| `status` | TEXT | `active`/`done`/`cancelled` |
| `notes` | TEXT | — |
| `target_price` | NUMERIC | سعر مستهدف |
| `reduction_pct` | NUMERIC | نسبة التخفيف % |
| `year` | INT | سنة المهمة |
| `auto_generated` | BOOLEAN | أنشأها النظام تلقائياً؟ |
| `closed_at` | TIMESTAMPTZ | وقت الإغلاق |
| `accumulate_at` / `trim_from` / `trim_to` / `liquidate_above` | NUMERIC | خطة الأسعار المستخدَمة في `targets.js` (تسجيل التقييم) وفي `decision-engine.js` (فلتر خطة الأسعار) |

> **ملاحظة معمارية:** توليد المهام التلقائي من انحرافات الأهداف لم يعد في `tasks.js` — انتقل منطق القرار إلى `decision-engine.js` الذي يقرأ `portfolio_tasks` كمُدخل بدل أن يكتب إليه.

#### `review_log` — سجل المراجعات

| العمود | النوع | الوصف |
|---|---|---|
| `id` | UUID PK | **محفوظ في الباكب** (FK مرجعية) |
| `ticker` | TEXT | رمز السهم |
| `review_date` | DATE | تاريخ المراجعة |
| `notes` | TEXT | نص المراجعة |

#### `review_log_attachments` — مرفقات المراجعات

| العمود | النوع | الوصف |
|---|---|---|
| `id` | UUID PK | **محفوظ في الباكب** |
| `entry_id` | UUID FK → review_log.id | — |
| `filename` | TEXT | اسم الملف |
| `ext` | TEXT | `txt`/`md`/`xlsx`/`csv` |
| `content` | TEXT | محتوى base64 أو نص |
| `size_bytes` | INT | الحجم (حد أقصى 2MB) |

#### `user_settings` — مصدر الحقيقة للبيانات المزامَنة عبر الأجهزة

| العمود | النوع | الوصف |
|---|---|---|
| `user_id` | UUID FK | — |
| `key` | TEXT | اسم الإعداد |
| `value` | TEXT (JSON) | القيمة بصيغة JSON |
| `updated_at` | TIMESTAMPTZ | — |

> PK مركّب على `(user_id, key)` — upsert تلقائي. **مفاتيح `key` المعروفة حالياً:**
> `salary_planner_v1` · `sukuk_planner_v1` · `retirement_goal_v1` · `school_tracker_v2` · `school_kanda_v1` · `tharwa-benchmark_v1` · `valuation_history_v1` · `decision_engine_v1` · `decision_engine_snapshot_v1` · `portfolio_rating_snapshot_v1`
> localStorage صار **cache قراءة أولى** فقط لهذه المفاتيح؛ `user_settings` هو مصدر الحقيقة الفعلي.

---

### ب) جداول الإدارة (10 جداول — محمية بـ `is_admin`)

#### `user_profiles` — ملفات المستخدمين

| العمود | النوع | الوصف |
|---|---|---|
| `id` | UUID PK = auth.users.id | — |
| `email` | TEXT | — |
| `status` | TEXT | `active`/`suspended`/`banned`/`deleted` |
| `last_seen` | TIMESTAMPTZ | يُحدَّث عند كل تسجيل دخول |
| `created_at` | TIMESTAMPTZ | — |

#### `site_config` — إعدادات الموقع

| العمود | النوع | الوصف |
|---|---|---|
| `key` | TEXT PK | مثال: `maintenance_mode` |
| `value` | TEXT | `true`/`false` أو نص |

#### `consent_logs` — سجلات الموافقة (غير قابلة للتعديل)

| العمود | الوصف |
|---|---|
| `user_id`, `email` | — |
| `consented_at` | وقت الموافقة |
| `ip_address` | — |
| `terms_version` | إصدار الشروط |

#### `data_erasure_requests` — طلبات حذف البيانات

| العمود | الوصف |
|---|---|
| `user_id`, `email` | — |
| `status` | `pending`/`executed`/`rejected` |
| `requested_at`, `executed_at` | — |

#### `deletion_requests` — طلبات حذف الحساب

| العمود | الوصف |
|---|---|
| `user_id`, `email`, `reason` | — |
| `created_at` | — |

#### `support_tickets` — تذاكر الدعم

| العمود | الوصف |
|---|---|
| `user_id`, `subject`, `description` | — |
| `browser` | بيانات المتصفح |
| `status` | `open`/`resolved` |
| `created_at`, `resolved_at` | — |

#### `admin_audit_logs` — سجل إجراءات المدير (غير قابل للتعديل)

| العمود | الوصف |
|---|---|
| `admin_id` | UUID المدير |
| `action_type` | مثال: `SUSPEND_USER`, `BLOCK_IP`, `EXECUTE_ERASURE` |
| `target_user_id` | المستخدم المستهدف |
| `action_details` | وصف الإجراء |
| `ip_address` | — |
| `created_at` | — |

#### `admin_broadcasts` — التنبيهات الجماعية

| العمود | الوصف |
|---|---|
| `admin_id` | — |
| `target` | `all`/`active`/`specific` |
| `subject`, `body` | — |
| `sent_at` | — |

#### `failed_login_attempts` — محاولات الدخول الفاشلة

| العمود | الوصف |
|---|---|
| `email`, `ip_address` | — |
| `attempt_count` | عدد المحاولات |
| `last_attempt` | — |

#### `blocked_ips` — عناوين IP المحظورة

| العمود | الوصف |
|---|---|
| `ip_address`, `email` | — |
| `blocked_at`, `blocked_by` | — |

---

## 4. localStorage

جميع المفاتيح مُقيَّدة بـ `userLsKey(k)` = `u:{userId}:{key}` لعزل بيانات كل مستخدم على نفس الجهاز. المفاتيح مشمولة في النسخة الاحتياطية **100%** (18 مفتاحاً في `LS_KEYS`):

| المفتاح | النوع | الوصف |
|---|---|---|
| `tharwa-theme` | string | `dark` / `light` |
| `tharwa-zoom` | string | حجم الخط px (افتراضي: 15) |
| `portfolio_cash_v1` | JSON | `{amount, updated_at}` — cache نقد المحفظة |
| `tharwa-alert-green` | string | عتبة التنبيه الأخضر % (افتراضي: 1) |
| `tharwa-alert-yellow` | string | عتبة التنبيه الأصفر % (افتراضي: 3) |
| `salary_planner_v1` | JSON | `{categories[], entries[]}` — مقسّم الراتب (cache؛ الحقيقة في `user_settings`) |
| `sukuk_planner_v1` | JSON | `{opportunities[]}` — بيانات الصكوك (cache) |
| `life_goals_v1` | JSON | `[{id,title,area,status,progress,...}]` |
| `inventory_v1` | JSON | `[{id,name,cat,loc,cond,...}]` |
| `school_tracker_v2` | JSON | `{children[{id,name,goals,years,grades,attendance,behavior,homework,exams...}]}` (cache) |
| `school_kanda_v1` | JSON | `{profile,lifeGoals,schoolGoals,years,subjects,grades}` (cache) |
| `nav_groups_v1` | JSON | حالة فتح/إغلاق مجموعات الناف بار |
| `retirement_goal_v1` | JSON | `{monthly, swr, target_year}` — هدف التقاعد / FIRE (cache) |
| `tharwa-price-timestamps` | JSON | `{ticker: ISO_timestamp}` — وقت آخر تحديث للسعر |
| `tharwa-benchmark_v1` | JSON | `[{date, value}]` — قيم مؤشر تاسي اليدوية (cache) |
| `tharwa-benchmark-seeded-v1` | string | `true` = تمت بذرة بيانات تاسي التاريخية (2004–2024) الأولى |
| `valuation_history_v1` | JSON | سجل عمليات حاسبة القيمة العادلة (cache؛ الحقيقة في `user_settings`) |
| `hide-salary-convention` | string | إخفاء لافتة "اتفاقية الفئات" في salary.html |
| `tharwa_emergency_backup` | JSON | نسخة طارئة تُحفظ تلقائياً قبل أي restore (محلي فقط، غير مُزامَن) |

---

## 5. تسجيل الدخول

**الملف:** `index.html` / `js/auth.js`

### وظائف auth.js

```javascript
requireAuth()
// 1. supabaseClient.auth.getSession()
// 2. إذا لا جلسة → redirect لـ index.html
// 3. يضبط window._currentUserId للاستخدام في userLsKey()
// 4. يُحدِّث last_seen في user_profiles
// 5. يُظهر رابط لوحة الإدارة إذا كان app_metadata.roles تتضمن 'admin'
// 6. يتحقق من maintenance_mode في site_config → redirect لـ maintenance.html
// 7. يُعيد user object

logout()
// supabaseClient.auth.signOut() + redirect لـ index.html
```

> **AUDIT-FIX (أمان):** فحص صلاحية المدير انتقل من `user_metadata.is_admin` (قابل للتعديل من طرف العميل نظرياً) إلى `app_metadata.roles` (لا يُكتب إلا من الخادم). راجع [قسم 34](#34-الأمان).

### عمليات تسجيل الدخول

| العملية | الدالة |
|---|---|
| تسجيل دخول | `supabaseClient.auth.signInWithPassword()` → redirect للداشبورد |
| إنشاء حساب | `supabaseClient.auth.signUp()` → بريد تأكيد |
| نسيت كلمة المرور | `supabaseClient.auth.resetPasswordForEmail()` → بريد استعادة |

### تدفق الأمان

```
كل صفحة:
  requireAuth() ─→ لا جلسة? ─→ index.html
                ─→ وضع صيانة + غير مدير? ─→ maintenance.html
                ─→ ✅ تحميل الصفحة
```

---

## 6. لوحة التحكم

**الملف:** `dashboard.html` / `js/dashboard.js`

### البيانات المُحمَّلة (Promise.all)

```
holdings, transactions, dividends, cashflow_entries,
net_worth_snapshots (آخر لقطة), real_estate,
stock_targets, sector_targets, portfolio_cash,
portfolio_tasks, user_stocks
```

### بانر تنبيهات إعادة التوازن

- يحسب انحراف كل سهم عن وزنه المستهدف
- يظهر تلقائياً إذا تجاوز أي سهم العتبة الخضراء
- ألوان: أصفر (> عتبة الأخضر) / أحمر (> عتبة الأصفر)
- يعرض أسوأ 4 انحرافات + رابط مباشر لصفحة إعادة التوازن
- يتحدث عند: فتح الصفحة، تحديث الأسعار، تعديل أي سهم

### بطاقات KPI — الصف الأول

| البطاقة | الحساب |
|---|---|
| إجمالي قيمة المحفظة | `Σ(shares × current_price) + portfolio_cash` |
| نقد المحفظة | من `portfolio_cash` — Supabase أو localStorage كـ fallback |
| قيمة العقارات | `Σ(current_value)` للعقارات غير المباعة |
| صافي الثروة | آخر قيمة من `net_worth_snapshots` |

### بطاقات KPI — الصف الثاني

**تبويب رأس المال:**
- رأس المال المنشغل = إجمالي شراء − إجمالي بيع (من `transactions.total`)
- تكلفة الوسيط (WAC) = `Σ(shares × avg_price)`
- **معدّل المساهمة الشهري (جديد):** `dep12 − wd12` (إيداعات وسحوبات آخر 12 شهراً من `cashflow_entries`) ÷ 12 — يُحسب فقط إذا وُجدت تدفقات مسجَّلة

**تبويبات العائد التوزيعي (4 تبويبات):**

| التبويب | الحساب |
|---|---|
| Forward (افتراضي) | وسيط آخر `freq` قيمة DPS × الدورية × الأسهم الحالية |
| العائد المُسنوى | أرباح السنة × (365÷الأيام المنقضية) ÷ رأس المال أول يناير |
| YOC | TTM ÷ تكلفة الحيازات × 100 |
| العائد السوقي | أرباح السنة مُسنواة ÷ القيمة السوقية |

**نمو الدخل التوزيعي السنوي (Dividend CAGR — جديد):**
```
divByYear[year] = Σ(كل التوزيعات في كل سنة تقويمية كاملة، تستبعد السنة الجارية)
إذا توفّرت سنتان مكتملتان على الأقل وأول سنة موجبة:
  span    = آخر سنة − أول سنة
  divCagr = ((divByYear[آخر] / divByYear[أول]) ^ (1/span) − 1) × 100
```

### XIRR — موقع الحساب

```
المدخلات: كل شراء (سالب)، بيع (موجب)، توزيع (موجب)، القيمة الحالية (موجب)
الخوارزمية: Newton-Raphson → Binary Search (في utils.js → computeXIRR)
المخرج: معدل العائد الداخلي السنوي الحقيقي %
الأرضية/السقف عند فشل Newton-Raphson (fallback CAGR): −5% إلى +40% سنوياً
```

### بطاقة هدف FIRE

**المدخلات:** المصاريف الشهرية + نسبة السحب الآمن SWR%

```
رقم FIRE (fireNumber)   = (المصاريف × 12) ÷ (SWR ÷ 100)
fireBase (الأصول المؤهلة) = قيمة الأسهم + نقد المحفظة + إجمالي الصكوك النشطة
                            (لا تشمل العقار — أصل غير سائل)
نسبة الإنجاز             = fireBase ÷ fireNumber × 100
المتبقي                  = fireNumber − fireBase
```

**التخزين:** `retirement_goal_v1` (Supabase `user_settings` + cache محلي)

### بطاقة إسقاط FIRE على المسار (جديد)

يجيب: هل مساهماتك الحالية كافية للوصول للهدف في السنة المحدَّدة؟

```
HEALTH_GROWTH = 5% سنوياً (نمو متحفّظ تخطيطي، منفصل عن XIRR الفعلي)
yearsLeft     = سنة الهدف − السنة الحالية
annualContrib = monthlyContrib × 12
g             = (1 + HEALTH_GROWTH) ^ yearsLeft

fvPv   = fireBase × g                              (القيمة المستقبلية للأصول الحالية)
fvPmt  = annualContrib × ((g − 1) / HEALTH_GROWTH)  (القيمة المستقبلية للمساهمات السنوية)
projAssets     = fvPv + fvPmt
projFireRatio  = projAssets ÷ fireNumber × 100
projCoverRatio = (projAssets × عائد التوزيع ÷ 12) ÷ الهدف الشهري × 100

النطاقات: ≥100% أخضر «على المسار» | 80–99% أصفر «قريب من المسار» | <80% أحمر «متأخر عن المسار»
```

الشرط: يحتاج `targetYear > 0`، `yearsLeft > 0`، وتدفقات نقدية مسجَّلة فعلياً.

### بطاقة مقياس التنويع (Diversification Gauge — جديد)

مبنية على مؤشر Herfindahl-Hirschman (HHI) ومرجع Evans & Archer (1968) عن العدد الفعّال للأسهم اللازم للتنويع الأمثل. الصيغة الكاملة (مشتركة عبر `utils.js → computeDiversification()`) موثّقة في [قسم 31](#31-الخوارزميات-الرئيسية). أهم عناصر البطاقة:
- حلقة تقدّم دائرية نحو هدف 15 سهماً فعّالاً
- شريطا توازن (توزيع الأسهم + توزيع القطاعات)
- تنبيه Diworsification (بيتر لينش) عند عدد الأسهم الفعلي > 30 سهماً بلا تحسّن حقيقي بالتنويع
- نافذة "تحليل مفصّل" (showDiversificationAnalysis) بقائمة تحقق

### بطاقة محلل صحة المحفظة (Portfolio Health — جديد)

تقييم مركّب على 4 محاور، بمرجعية Graham/Lynch/نظرية المحفظة الحديثة (MPT):

| المحور | القاعدة |
|---|---|
| **التنويع** | <5 سهم=🔴 · 5–9=🟡 · 10–20 مع ≥4 قطاعات=🟢 · >25=🟡 (Diworsification) |
| **التركيز** | نسبة أكبر سهم + أكبر 3 أسهم + أكبر قطاع |
| **تغطية الدخل الشهري** | دخل توزيعي متوقع (Forward) ÷ الهدف الشهري × 100 |
| **التقدم نحو FIRE** | من بطاقة إسقاط FIRE أعلاه |

نافذة `showHealthInfo` تشرح المراجع العلمية والحدود المستخدمة.

### جدول الحيازات

**الأعمدة:** الرمز | الاسم | القطاع | الأسهم | متوسط التكلفة | السعر الحالي | تكلفة الحيازة | القيمة السوقية | ر/خ % | الوزن | المستهدف | حالة التنبيه | آخر تحديث

**تعديل inline:** كل خلية قابلة للتعديل بنقرة (مع allowlist أمان في utils.js)

**تحديث الأسعار:**
- يدوي: نقرة على خلية السعر
- تلقائي: Supabase Edge Function `update-prices`
- تنبيه أسعار قديمة: أكثر من 7 أيام بدون تحديث (`STALE_DAYS = 7`)
- تنبيه فشل التحديث: toast يُظهر رموز الأسهم التي لم تُحدَّث
- تنبيه مناطق الشراء/البيع: نافذة عند وصول السعر للمنطقة (من `accumulate_at`/`trim_from`/`liquidate_above`)

### مزامنة المحفظة من المعاملات

```
recomputeHoldingFromTx(ticker):
  يعيد حساب holdings.shares و holdings.avg_price من الصفر
  يحافظ على: current_price, sector, target_weight
  يُصحح: shares + WAC
  زر "إعادة حساب" يدوي متاح في جدول الحيازات (syncHoldingsFromTx)
```

### التسجيل التلقائي للقيمة (Auto Snapshot)

- مرة واحدة كل شهر عند فتح الداشبورد
- يحفظ في `net_worth_snapshots` بـ `notes = "auto-YYYY-MM"`
- يبني تاريخ أداء حقيقي لصفحة الأداء

### بطاقة نقطة التعادل الموسّعة (Break-Even — محدَّثة)

4 أنماط عرض: ملخص / تفصيل / مساهمة (أشرطة) / مخطط.

```
رأس المال المنشغل   = إجمالي المشتريات − إجمالي المبيعات
إجمالي العوائد      = قيمة المحفظة الحالية + إجمالي التوزيعات المستلمة
صافي الربح/الخسارة  = إجمالي العوائد − رأس المال المنشغل
نسبة استرداد رأس المال = إجمالي العوائد ÷ رأس المال المنشغل × 100  (محدودة بـ 200% للعرض)

النطاقات: ≥100% أخضر (تعدّى التعادل) | 75–99% أصفر | <75% أحمر
```

> **AUDIT-FIX (2026-07):** كانت حصيلة البيع تُحسب مرتين (تُخصم من رأس المال المنشغل ثم تُضاف مجدداً كـ"نقد عائد"). الآن حصيلة البيع مخصومة أصلاً ضمن رأس المال المنشغل ولا تُضاف مجدداً في إجمالي العوائد.

### نظام موثوقية الأرقام (شارات 🟢🟡🔵)

- 🟢 عالية: محسوبة مباشرة من بيانات فعلية
- 🟡 متوسطة: تعتمد افتراضاً (سعر محدَّث، SWR، تقدير زمني)
- 🔵 إرشادي: مؤشر توجيهي فقط، ليس رقماً دقيقاً

### نافذة معلومات البطاقات (showCardInfo)

كل بطاقة KPI تحمل زر ⓘ يستدعي `showCardInfo(key)` من `utils.js` (راجع [قسم 33](#33-utils)) لعرض منهجية الحساب داخل البطاقة نفسها.

### الرسوم البيانية

| الرسم | الأوضاع | الوصف |
|---|---|---|
| توزيع القطاعات | donut / bars / cards | الوزن الحالي لكل قطاع |
| خريطة الأوزان | bars / gap / cards / table | الحالي vs المستهدف |
| الدخل حسب القطاع | stacked bar | توزيع الأرباح المتوقعة |
| تخصيص الأصول | pie | أسهم + عقارات + نقد + صكوك + أصول |
| نقطة التعادل | summary / detail / بارز contribution / بارز chart | متى يتعادل كل سهم بالأرباح |

---

## 7. سجل المعاملات

**الملف:** `transactions.html` / `js/transactions.js`

### إضافة معاملة فردية

| الحقل | النوع | ملاحظة |
|---|---|---|
| التاريخ | date | افتراضي: اليوم |
| رمز السهم | text | يملأ الاسم تلقائياً؛ **تنبيه إذا الرمز غير معروف** (AUDIT-FIX: كان يعمل في الإضافة الجماعية فقط، الآن في الفردية أيضاً) |
| النوع | select | شراء / بيع / أسهم منحة |
| عدد الأسهم | number | — |
| السعر | number | يُقفل = 0 للمنحة |
| العمولة | number (auto) | محسوبة من `calcCommission()` |
| VAT | number (auto) | محسوبة |
| الإجمالي | number (auto) | محسوب |

**حساب العمولة:**
```
COMMISSION_RATE = 0.0015  (قابل للتعديل — راجع عقد الوسيط)
commission = min( shares × price × COMMISSION_RATE , 100 )
vat        = commission × 0.15
total_buy  = shares × price + commission + vat
total_sell = shares × price − commission − vat
total_grant = 0
```

**تأثير على holdings:**
- شراء: يزيد الأسهم + يُعيد حساب WAC
- منحة: يزيد الأسهم بدون تغيير avg_price (تكلفة صفر)
- بيع: يُنقص الأسهم، يحذف السهم عند الصفر

### إضافة جملة (Bulk Staging)

- جدول تفاعلي بأي عدد صفوف
- كل صف يحسب العمولة/الإجمالي تلقائياً
- حفظ دفعة واحدة

### تعديل / أرشفة معاملة

- تعديل → modal → بعد الحفظ: `recomputeHoldingFromTx()` يُعيد الحساب من الصفر
- **AUDIT-FIX:** تعديل حقل `date` بمفرده يُشغّل `recomputeHoldingFromTx()` أيضاً (كان يُهمَل سابقاً رغم أن التاريخ يغيّر ترتيب البيع/الشراء وبالتالي WAC)
- أرشفة: `is_archived = true` — لا حذف حقيقي أبداً
- بعد الأرشفة: `recomputeHoldingFromTx()` يُصحح holdings

### إحصائيات KPI

إجمالي العمليات | شراء + مبلغها | بيع + مبلغها | صفقات رابحة | صفقات خاسرة

---

## 8. الأرباح الموزعة

**الملف:** `dividends.html` / `js/dividends.js`

### المدخلات

| الحقل | الوصف |
|---|---|
| التاريخ | تاريخ الاستلام |
| الرمز | يملأ الاسم تلقائياً |
| المبلغ | الإجمالي المستلم (ليس للسهم الواحد) |
| الشهر | شهر التوزيع |
| السنة | سنة التوزيع |

### مؤشرات KPI

| المؤشر | الحساب |
|---|---|
| إجمالي الأرباح | `Σ(amount)` كل الأوقات |
| أرباح السنة الجارية | `Σ(amount)` للسنة الحالية |
| TTM | `Σ(amount)` آخر 12 شهراً فعلياً |
| YOC الفعلي | TTM ÷ تكلفة الحيازات × 100 |
| Forward | `Σ(DPS_وسيط × دورية × أسهم حالية)` لكل سهم — **موحّد الآن مع dashboard.js عبر نفس الدالة `_projectedAnnualIncome()`** |

**حساب Forward بالتفصيل (مُحدَّث):**
```
لكل سهم محتفظ به:
  1. الدورية من وسيط الفجوات الزمنية بين كل التوزيعات المتتالية:
     وسيط الفجوة ≤105 يوم → ربع سنوي (×4)
     وسيط الفجوة ≤210 يوم → نصف سنوي (×2)
     وسيط الفجوة >210 يوم → سنوي (×1)
  2. DPS = وسيط (median) آخر (الدورية) قيمة DPS، وليس آخر قيمة واحدة فقط
     — يمنع تضخيم العائد بسبب توزيعة أخيرة استثنائية (مثال: 1,1,1,5 لا ترفع forward إلى 5×4)
  3. Fallback عند غياب بيانات كافية لاشتقاق DPS من الأسهم وقت كل توزيعة:
     DPS = آخر سنة مسجَّلة ÷ الدورية ÷ الأسهم الحالية (لا الإجمالي التراكمي، تجنّباً للتضخيم)
  4. Projected = DPS × الدورية × الأسهم الحالية
```

### الملخص السنوي

- عائد كل سنة = أرباح السنة ÷ رأس المال أول يناير
- السنة الجارية: عائد جزئي فعلي (بدون توسيع)

### درجة جودة التوزيعات (0–100 لكل سهم)

```
استمرارية (35): السنوات الفعلية ÷ السنوات المتوقعة × 35
نمو (35):       CAGR آخر 3–5 سنوات (≥8% → 35 | <−10% → 0)
ثبات (30):      (1 − CV) × 30  [CV = انحراف معياري ÷ متوسط]
```

### مؤشر ثقة البيانات (Data Confidence Banner)

```
الدرجة = عمر رأس المال (45%) + سنوات الأرباح (35%) + تغطية Forward (20%)

عمر رأس المال (agePct — عبر Capital-Weighted Age، أشهر مرجَّحة بمبالغ الإيداع):
  <3 شهر=5% | <6=22% | <9=35% | <12=50% | <18=67% | <24=80% | <36=90% | ≥36=100%

سنوات الأرباح (divPct — دورات مقيَّدة بعمر المحفظة):
  maxCycles = ceil(عمر_المحفظة_بالأشهر ÷ 12)
  divYears  = min(عدد السنوات التقويمية الفعلية بالتوزيعات, maxCycles)
  0 سنوات=5% | 1=48% | 2=75% | ≥3=95%

تغطية Forward (covPct — نسبة الأسهم الموزِّعة المُدرَجة في الحساب):
  0 رموز=10% | <50%=50% | <80%=75% | ≥80%=95%
```

> **قاعدة التقييد الحرجة:** `divYears` (عدد سنوات جودة/ثقة التوزيعات) لا يتجاوز أبداً `ceil(عمر المحفظة بالأشهر ÷ 12)` — يمنع تضخيم الثقة لمحفظة عمرها أقل من سنة لكنها امتدت على سنتين تقويميتين (مثال: شراء في ديسمبر 2025 وتوزيعة في يناير 2026 لا تُحتسب "سنتان").

### تكلفة الحيازات المستخدَمة في المقاييس

```
avg_cost   = Σ(price × shares للمشتريات) ÷ Σ(shares المشتراة)   [يستخدم t.total الشامل للعمولة/الضريبة]
costBasis  = avg_cost × (أسهم مشتراة − أسهم مباعة)
```

---

## 9. التدفقات النقدية

**الملف:** `cashflows.html` / `js/cashflows.js`

### المدخلات

| الحقل | الخيارات |
|---|---|
| التاريخ | — |
| النوع | إيداع / سحب |
| المبلغ | — |
| الملاحظات | اختياري |

### الوظيفة الأساسية

تسجيل كل مبلغ يدخل/يخرج من حساب المحفظة — يُستخدم لـ:
- صافي رأس المال المُودَع (Capital Invested)
- حساب `buildCumulativeCapitalMap()` في performance.js
- Capital-Weighted Age في forecast.js و dividends.js
- معدّل المساهمة الشهري في dashboard.js

### إحصائيات

| المؤشر | الحساب |
|---|---|
| إجمالي الإيداعات | `Σ(amount)` حيث `type=deposit` |
| إجمالي السحوبات | `Σ(amount)` حيث `type=withdrawal` |
| صافي رأس المال | إيداعات − سحوبات |
| أرباح موزعة (عرض فقط) | قراءة فقط من dividends |

> **AUDIT-FIX:** نافذة تأكيد الحذف استُبدلت بـ `confirmAsync()` (متوافقة مع الجوال وسياسة CSP) بدل `confirm()` المتصفح الأصلي.

---

## 10. قاعدة بيانات أسهمي

**الملف:** `userdb.html` / `js/userdb.js`

قاعدة بيانات شخصية للأسهم المتابَعة — مستقلة عن المحفظة الفعلية.

### المدخلات

| الحقل | الوصف |
|---|---|
| الرمز | يملأ الاسم والقطاع تلقائياً من TICKER_DB (285 رمزاً) |
| الاسم | — |
| القطاع | dropdown من OFFICIAL_SECTORS |

### الوظائف

- إضافة / تعديل / حذف أسهم
- عند تعارض رمز موجود: نافذة تأكيد
- الأسهم الموجودة في holdings تظهر ببادج "في المحفظة"
- تُستخدم في targets.html كـ "أسهم مخططة"

> **AUDIT-FIX:** تعديل القطاع عبر `prompt()` استُبدل بنمط نافذة DOM آمن (متوافق مع CSP والجوال).

---

## 11. أهداف الأسهم والقطاعات

**الملف:** `targets.html` / `js/targets.js`

### أ) أهداف الأسهم

**المدخلات لكل سهم:**

| الحقل | الوصف |
|---|---|
| منطقة الشراء | سعر الدخول المستهدف (تُشتق من مهمة نشطة `accumulate_at` إن وُجدت، وإلا تبقى القيمة المحفوظة) |
| منطقة البيع | سعر الخروج المستهدف (تُشتق من `liquidate_above` بنفس المنطق) |
| الوزن المستهدف % | يُحفظ لكل سهم بطلبات Supabase متوازية (`Promise.all`) بدل حلقة متسلسلة |

**المخرجات:**
- الوزن الحالي % = `(shares × current_price) / totalValue × 100`
- حالة التنبيه: أخضر (≤ G%) / أصفر (≤ Y%) / أحمر (> Y%)

**التحقق عند الحفظ:**
- إجمالي الأهداف ≤ 100%
- منطقة الشراء < منطقة البيع

### ب) أهداف القطاعات

نفس المنطق للقطاعات — الوزن محسوب من `holdings.sector`

### ج) محرك إعادة التوازن (مُحدَّث — 3 طرق توزيع + تسجيل تقييم)

**المدخلات:**
- المبلغ المتاح (ر.س)
- طريقة التوزيع: **بالتناسب (gap) / متساوٍ (equal) / الأولوية للأبعد (top1)**
- تفعيل "مراعاة التقييم" (valuation-aware) — اختياري
- فلتر منطقة الشراء (اختياري)

**المرشحون:** أسهم مملوكة فعلاً (من `holdings`، وليس المخطط في `user_stocks`/`watchlist` الذي لا سعر حي له) بفجوة عن الهدف > 0.05% وسعر حالي > 0.

**درجة التقييم (valuationScore — إذا فُعِّلت مراعاة التقييم):**
```
مصدر 1 (وزن 70%) — من مناطق المهام (portfolio_tasks: accumulate_at, trim_from, liquidate_above):
  السعر ≥ liquidate_above           → 0   (متضخم)
  السعر ≤ accumulate_at             → 1   (جذاب)
  السعر ≥ trim_from                 → 0   (متضخم)
  بين accumulate_at و trim_from     → استيفاء خطي بينهما
  [عند وجود accumulate_at فقط: سقف = accumulate_at×1.15 | عند trim_from فقط: أرضية = trim_from×0.85]

مصدر 2 (وزن 30%) — من آخر تقييم في valuation_history_v1:
  الهامش = (القيمة_العادلة − السعر) ÷ القيمة_العادلة
  درجة_FV = clamp((الهامش + 0.10) ÷ 0.40, 0, 1)   [عند −10%=0 | عند +30%=1]

الدرجة النهائية = 0.7×درجة_Zones + 0.3×درجة_FV
(إن غاب أحد المصدرين تُستخدم الآخر منفرداً؛ إن غابا معاً: درجة محايدة = 0.5)

إن كانت كل درجات المرشحين ≤ 0.05 (قريبون من منطقة تضخّم) → المحرك يُصرّح: "لا شراء مُوصى به الآن"
```

**طرق التوزيع الثلاث:**

```
1) بالتناسب (gap):
   أولوية_i = الفجوة_i × (درجة_التقييم_i أو 1.0 إذا التقييم غير مفعّل)
   المخصص_i = الميزانية × (أولوية_i ÷ Σ الأولويات)  محدوداً بـ maxAlloc_i

2) متساوٍ (equal):
   المؤهلون = (إذا التقييم مفعّل: المرشحون بدرجة > 0.15) وإلا كل المرشحين
   المخصص_i = الميزانية ÷ عدد المؤهلين   محدوداً بـ maxAlloc_i

3) الأولوية للأبعد (top1):
   المختار = أعلى مرشح أولوية فقط
   المخصص = min(الميزانية, maxAlloc_المختار)

الحد الأقصى للتخصيص (maxAlloc) — يمنع تجاوز الهدف بعد الشراء:
  إجمالي_جديد = إجمالي_المحفظة + الميزانية
  maxAlloc_i  = max(0, (الهدف%_i ÷ 100) × إجمالي_جديد − القيمة_الحالية_i)

أسهم للشراء = floor(المخصص ÷ السعر الحالي)
التكلفة    = أسهم_للشراء × السعر
```

**المخرج:** جدول — الرمز | أسهم تشتري | التكلفة | الوزن قبل/بعد | الفجوة المتبقية

---

## 12. أسهم تحت المراقبة

**الملف:** `watchlist.html` / `js/watchlist.js` (نما 4 أضعاف — أسعار حيّة + محاكي أثر التنويع)

### المدخلات

| الحقل | الوصف |
|---|---|
| الرمز | يملأ الاسم والقطاع من user_stocks ثم TICKER_DB |
| سعر الاستهداف | سعر الدخول المستهدف |
| النسبة المخطط % | — |
| الملاحظات | — |

محفوظ في Supabase → مشمول في الباكب.

### أسعار اليوم اللحظية (جديد)

```
عند التحميل وبعد كل إضافة/تعديل: supabaseClient.functions.invoke('update-prices', {tickers})
عرض السعر: ⏳ أثناء التحميل، — عند الفشل
إن وُجد سعر مستهدف: أخضر (السعر ≤ الهدف، فرصة) / برتقالي (أعلى من الهدف بنسبة X%)
لا يُحفظ في holdings — عرض فقط
```

### محاكي أثر التنويع (Impact Analysis — جديد بالكامل)

يجيب: لو أضفت هذا السهم للمحفظة الآن، هل يحسّن التنويع أم يزيد التركيز؟

```
1. محاكاة الإضافة:
   إن كان السهم مملوكاً بالفعل:
     الوزن المخطط = planned_pct÷100 (أو 1÷(n+1) إن لم يُحدَّد)
     قيمة_الإضافة = (الوزن × (إجمالي − قيمة_حالية)) − قيمة_حالية
   إن كان جديداً:
     قيمة_الإضافة = الوزن × إجمالي ÷ (1 − الوزن)
   مواضع_محاكاة = holdings + {السهم بقيمة_الإضافة}

2. القياس قبل/بعد عبر computeDiversification() (نفس دالة الداشبورد):
   δgaugePos = gaugePos_بعد − gaugePos_قبل

3. فحوصات إضافية:
   وزن_السهم_المستقبلي، وزن_القطاع_قبل/بعد
   قطاع_جديد؟ = القطاع غير موجود أصلاً في المحفظة
   يتجاوز_هدف_القطاع؟ = وزن_القطاع_بعد > الهدف + 0.05
   مركز_كبير؟ = وزن_السهم_بعد > 15%

4. الحكم:
   (تجاوز هدف قطاع) أو (مركز>15%) أو (δ≤−2):
     قطاع جديد و δ≥0 و ليس مركزاً كبيراً → 🟠 تحفّظ
     وإلا → 🔴 سلبي / يزيد التركيز
   قطاع جديد أو δ≥2 → 🟢 إيجابي / يحسّن التنويع
   غير ذلك → 🟡 محايد
```

نافذة تفصيلية (openImpactModal) تعرض السبب + مقاييس قبل/بعد (المؤشر، N الفعّال، عدد الأسهم/القطاعات).

### بطاقة سياق المحفظة الحالية

تعرض مؤشر التنويع الحالي (X/100)، العدد الفعّال، عدد الأسهم/القطاعات، وأكبر مركز — كمرجع مقارنة قبل إضافة أي سهم من المراقبة، مع رابط لصفحة الداشبورد الكاملة.

### تأكيد: إعادة التوازن على المملوك فقط

محرك التوازن في `targets.js` يعمل حصراً على `holdings` الفعلية — الأسهم "المخططة" (من `user_stocks`/`watchlist`) لا تملك `current_price` حياً فلا يمكن حساب عدد أسهم قابل للشراء لها. هذا سلوك مؤكَّد ومقصود.

---

## 13. الرؤية المستقبلية

**الملف:** `forecast.html` / `js/forecast.js`

### البيانات التاريخية المحسوبة تلقائياً

```
القيمة الحالية      = Σ(shares × current_price)
التكلفة الأساسية   = Σ(shares × avg_price)
XIRR               = معدل العائد الداخلي الحقيقي
safeDivYield       = متوسط آخر سنتين ÷ القيمة الحالية
annCapGrowth       = XIRR − safeDivYield  (fallback: CAGR)
Capital-Weighted Age = عمر رأس المال المرجّح بالتدفقات
```

### نظام DCA متعدد الفترات (جديد)

بدل رقم إضافة شهرية واحد ثابت، يمكن تعريف عدة فترات متتالية (مثال: 8,000 ر.س × 5 سنوات ثم 12,000 ر.س × 10 سنوات):

```
buildDcaSchedule(periods, totalMonths=420 [35 سنة]):
  cursor = 0
  لكل period في periods:
    امْلأ الجدول[cursor : cursor+period.months] = period.amount
    cursor += period.months
  (الأشهر المتبقية بعد آخر فترة تستمر بآخر مبلغ معرَّف)
```

### معايرة السيناريوهات على أداء تاسي الفعلي (2004–2024)

```
بيانات إغلاق نهاية السنة الفعلية لمؤشر تاسي (TASI_PRICE_YE)، 21 سنة
CAGR يُحسب على نوافذ متداخلة 10/15/20 سنة (لا سنة واحدة — تقلب سنوي يصل من −57% إلى +104%)
تُستبعد بدايات النوافذ من سنة 2005 (فقاعة منفوخة انهارت −52% سنة 2006) لتفادي انحياز القاعدة

MARKET_CAP_BENCHMARK = 4.4% سنوياً  (معيار تاسي 2010–2024)
MARKET_LOW            ≈ 2.4%        (قاع فترات بطيئة كـ2017/2020/2024)
```

### 3 سيناريوهات (السيناريو الاستثنائي حُذف لعدم واقعيته)

| السيناريو | نمو رأس المال | عائد الأرباح | المصدر |
|---|---|---|---|
| متحفظ 🛡️ | `max(0, min(base×0.8, MARKET_LOW))` | `div×0.80` | ارتداد للمتوسط |
| معتدل 📊 | `blendedCapGrowth` (أداؤك ⨉ ثقة + تاسي ⨉ (1−ثقة)) | `safeDivYield` | مزيج أدائك الفعلي وتاسي |
| متفائل 🚀 | `min(0.12, base+0.025)` | `min(0.06, div+0.01)` | ربع أعلى واقعي |

**المزج الواقعي للسيناريو المعتدل:**
```
blendedCapGrowth = (أداؤك الشخصي × درجة_الثقة) + (معيار_تاسي 4.4% × (1 − درجة_الثقة))
عند ثقة 0% (محفظة جديدة): السيناريو المعتدل = 4.4% (تاسي فقط)
عند ثقة 100%: السيناريو المعتدل = أداؤك الشخصي بالكامل
السقف: 11% سنوياً | الأرضية: 0%
```

**احتمالات ظهور السيناريو (scenarioOccurrenceProbs):** نسبة الفترات التاريخية (نوافذ 10–20 سنة) التي وقع نموّها فعلياً في جوار كل سيناريو من أصل نوافذ تاسي التاريخية، إضافة لنسبة الفترات التي كانت خسارة سعرية بحتة (غير مُغطاة بأي سيناريو).

### مؤشر ثقة البيانات (Confidence Score 0–100)

```
الدرجة = عمر رأس المال المرجَّح (45%) + دورات الأرباح (35%) + عدد الأسهم (20%)

عمر رأس المال (Capital-Weighted Months):
  Σ(مبلغ_إيداع × أشهر_في_المحفظة) ÷ رأس_المال_الحالي
  مثال: 10K (8 أشهر) + 170K (4 أشهر) = (80K+680K)/180K = 4.2 شهر فعلي
  fallback: تواريخ transactions إذا لا cashflow_entries
  السقف 45%: <3 شهر=5% | 3–6=20% | 6–9=... | >36=100%

دورات الأرباح: نفس تقييد ceil(عمر المحفظة بالأشهر÷12) الموثّق في قسم 8
  السقف 35%: 0=5% | 1=45% | 2=72% | ≥3=95%

عدد الأسهم: السقف 20%: <3=40% | 3–6=65% | 6–10=82% | ≥10=95%
```

**مستويات الثقة:** very_low(<30%) · low(30–44%) · developing(45–59%) · fair(60–74%) · good(75–86%) · strong(87–100%) — كل مستوى له نص إرشادي مختلف عن مدى الاعتماد على الإسقاط.

### المدخلات اليدوية

| الحقل | الافتراضي | الوصف |
|---|---|---|
| القيمة الحالية | من البيانات | نقطة البداية |
| فترات إضافة شهرية (متعددة) | متوسط الإيداعات | DCA بجدول زمني |
| مبلغ فوري | 0 | إضافة فورية |
| أفق زمني | 35 سنة | — |
| إعادة استثمار الأرباح | ✅ | — |
| تعديل التضخم | ❌ | معدل التضخم % |
| عائد الأرباح (تجاوز) | من البيانات | يُلغي الحسابي |
| الهدف | 0 | قيمة محفظة أو دخل شهري |

### ربط هدف FIRE من الداشبورد

```
تحميل retirement_goal_v1 → زر "تطبيق هدف FIRE" يملأ الحقول تلقائياً:
  fireNumber = (monthly × 12) ÷ (swr ÷ 100)
  إن بقيت سنوات للتقاعد وتفعّل التضخم:
    inflatedMonthly = monthly × (1+معدل_التضخم)^سنوات_متبقية
    fireInflated     = (inflatedMonthly × 12) ÷ (swr ÷ 100)
  يُعيّن نوع الهدف = قيمة المحفظة، ويُقرِّب الأفق الزمني لأقرب خيار متاح
```

### محرك الإسقاط الشهري

```
لكل شهر:
  value *= (1 + monthlyCapRate)        // monthlyCapRate = (1+annualCapRate)^(1/12) − 1
  div_earned = value × monthly_div_rate  // أرباح الشهر
  if reinvest: value += div_earned       // إعادة الاستثمار
  value += dcaSchedule[m]                // DCA حسب الجدول متعدد الفترات
  if inflation: factor *= (1 + monthly_inf)
  كل 12 شهراً: لقطة سنوية (قيمة اسمية + حقيقية + دخل شهري + رأس مالك المضاف)
```

### حساب سنة الوصول للهدف (بقوة الشراء الحقيقية)

```
computeGoalYear: الهدف يُقاس بقوة شراء اليوم دائماً
  realMetric = nominal ÷ (1+معدل_التضخم)^السنة   (إذا التضخم مفعّل، وإلا = nominal)
  أول سنة realMetric ≥ الهدف = سنة الوصول
خط الهدف على الرسم يُعرض اسمياً: goalLine[y] = goalAmount × (1+معدل_التضخم)^y (متوافق مع الرقم المُرجَع)
```

### المخرجات

- رسم خطي / لوغاريتمي / شريطي (معالم فقط) / بطاقات (شبكة سيناريو × سنة)
- جدول المعالم: قيمة + دخل شهري لكل سنة (اسمي وحقيقي)، صفوف مظلّلة كل 5 سنوات، تعليم سنة هدف FIRE بـ 🎯
- لوحة الهدف: متى يصل كل سيناريو للهدف
- تفاصيل السيناريو (عند اختياره): 13 مؤشراً — النمو والعائد السنوي، القيمة الحالية، رأس المال المضاف، الأرباح المتراكمة، سنة الوصول، الدخل عند الهدف، القوة الشرائية الحقيقية، شريط توزيع (رأس مال/أرباح/نمو)، عمر المحفظة الفعلي ودرجة الثقة

---

## 14. الأداء التاريخي

**الملف:** `performance.html` / `js/performance.js`

### البيانات المُحمَّلة

```
transactions, holdings, dividends, cashflow_entries, net_worth_snapshots
```

### ملاحظة معمارية — كاش buildPositionData

```javascript
// يُحسَب مرة واحدة لكل تحميل — يُستخدم من عدة دوال render
let _positionCache = null;
getPositionData()  // ← الواجهة الموحدة
buildPositionData() // ← الحساب الفعلي (يُستدعى مرة فقط)
// يُبطَّل الكاش عند إعادة تحميل البيانات
```

### تبويب 1 — المراكز المفتوحة

```
cost_of_remaining = avg_price × remaining_shares
market_value      = current_price × remaining_shares
unrealized_pnl    = market_value − cost_of_remaining
unrealized_pct    = unrealized_pnl ÷ cost_of_remaining × 100
partial_realized  = sell_revenue − (buy_avg × sold_shares)
total_return      = unrealized + partial_realized + dividends
```

**إصلاح حساب الربح المحقَّق (AUDIT-FIX):** بدل قسمة تكلفة الشراء الكلية على إجمالي الأسهم المباعة (خطأ يخلط مشتريات لاحقة)، الحساب الآن يمرّ زمنياً على كل حدث شراء/بيع مرتَّب بالتاريخ ويحدّث متوسط التكلفة (WAC) لحظة كل عملية بيع:
```
لكل حدث (شراء/بيع/منحة) مرتَّب زمنياً:
  إن بيع:
    avg = رصيد_التكلفة_الحالي ÷ الأسهم_الحالية
    الربح_المحقق += (حصيلة_البيع − avg × الأسهم_المباعة)
    رصيد_التكلفة -= avg × الأسهم_المباعة
  إن شراء أو منحة:
    رصيد_التكلفة += إجمالي_الشراء   (منحة: 0 — لا تكلفة)
    الأسهم += أسهم_العملية
```
هذا يوحّد النتيجة تماماً بين لوحة التحكم وسجل المعاملات وصفحة الأداء.

### تبويب 2 — المراكز المغلقة

```
realized_pnl   = sell_revenue − buy_cost
realized_pct   = realized_pnl ÷ buy_cost × 100
total_return   = realized_pnl + dividends_received
hold_days      = parseDateLocal(last_sell) − parseDateLocal(first_buy)
```

> **ملاحظة أسهم المنحة:** grant لا تُضيف تكلفة لـ `buyCost` → `avgCost = buyCost ÷ total_shares` يتأثر بالأسهم الممنوحة ولكنه صحيح اقتصادياً (تكلفة صفر).

### تبويب 3 — التايم لاين الشهري

لكل شهر منذ أول معاملة:
- رأس المال التراكمي: من `buildCumulativeCapitalMap()` (prefix-sum O(M) لا O(N×M))
- قيمة المحفظة: من أقرب `net_worth_snapshot`
- أرباح الشهر | مشتريات | مبيعات | صافي الحركة

### تبويب 4 — الرسم البياني الشهري (4 أوضاع)

- مدمج: خط رأس المال + قيمة المحفظة + أعمدة الأرباح/المشتريات
- خطوط: رأس المال + أرباح تراكمية + مشتريات تراكمية
- مكدس: مشتريات + أرباح + مبيعات لكل شهر
- أرباح فقط: الشهري + التراكمي

### تبويب 5 — مقارنة بمؤشر تاسي

**المدخلات اليدوية:** تاريخ + قيمة تاسي

**خوارزمية المقارنة:**
```
1. ادمج تواريخ تاسي + مواعيد snapshots
2. لكل تاريخ: أقرب قيمة تاسي سابقة + أقرب snapshot سابق
3. احتفظ فقط بالنقاط ذات القيمتين معاً
4. نسّب كلاهما إلى 100 عند أول نقطة مشتركة
5. Alpha = عائد المحفظة − عائد تاسي
```

### تبويب 6 — مؤشرات التوزيعات المتقدمة (جديد)

```
YoC (العائد على التكلفة) = Forward Annual ÷ تكلفة الحيازة الأصلية × 100
Current Yield            = Forward Annual ÷ القيمة السوقية الحالية × 100
Div ROI                  = إجمالي التوزيعات المستلمة تراكمياً ÷ إجمالي تكلفة الشراء × 100
Break-Even Years         = تكلفة الحيازة الحالية ÷ التوزيع السنوي المتوقع
Portfolio Efficiency Ratio = (الأرباح المحققة + غير المحققة) ÷ إجمالي العمولات والضرائب
  التقييم: ≥20× ممتاز | 10–20× جيد | <10× منخفض
```

### تبويب 7 — تحليل السلوك الاستثماري (Behavioral Audit — جديد)

```
Win Rate       = عدد الصفقات الرابحة ÷ إجمالي الصفقات المغلقة × 100
  ≥60%=ممتاز | 40–60%=معقول | <40%=منخفض

Profit Factor  = إجمالي أرباح الصفقات الرابحة ÷ إجمالي خسائر الصفقات الخاسرة
  >2=ممتاز | 1–2=مقبول | <1=خطر (خسائر أكبر من أرباح)

متوسط أيام الاحتفاظ (رابحة مقابل خاسرة):
  إن (متوسط أيام الخاسرة) > (متوسط أيام الرابحة) × 1.3 → تشخيص "نفور من الخسارة" (Loss Aversion)

Risk/Reward Ratio = متوسط ربح الصفقة الرابحة ÷ متوسط خسارة الصفقة الخاسرة
  ≥1.5×=ممتاز | 1–1.5×=جيد | <1×=سيء

وتيرة التداول = (عدد الشراءات + المبيعات) ÷ عدد أشهر النشاط
  >8=مرتفع جداً (مضارب) | 4–8=متوسط | <4=منضبط (مستثمر)

+ توزيع الصفقات الشهري (رسم) + أفضل/أسوأ 3 صفقات مغلقة (عائد% + مدة الاحتفاظ)
```

### مؤشر تركيز المحفظة (HHI — جديد، يُستخدم أيضاً في dashboard/watchlist)

```
HHI = Σ(وزن_السهم)²
Effective N = 1 ÷ HHI
HHI<0.10 → تنويع ممتاز (Eff N>10) | HHI<0.18 → معقول (~5.5) | HHI<0.25 → متوسط ⚠️ | ≥0.25 → عالٍ ❌
```

### Max Drawdown — معايرة محسّنة عبر TWR

**المنهج السابق (غير دقيق):** من `net_worth` الخام مباشرة — يخلط أثر الإيداعات/السحوبات مع أداء السوق الفعلي.

**المنهج الحالي:** يُحسب من عائد مُعدَّل بالزمن (Time-Weighted Return) بطريقة Modified Dietz، تزيل أثر التدفقات النقدية أولاً:
```
لكل فترة بين لقطتين:
  r = (القيمة_النهاية − القيمة_البداية − صافي_التدفق) ÷ (القيمة_البداية + صافي_التدفق÷2)
  factor *= (1 + r)   // مؤشر سعري تراكمي يبدأ من 100

Max Drawdown من كل قمة (peak) إلى أدنى قاع (trough) لاحق على مؤشر factor
تلوين: أحمر <−15% | أصفر −8% إلى −15% | أخضر ≥−8%

Deduplication: يُبقي على لقطة واحدة فقط لكل يوم (يُفضَّل اليدوي على التلقائي)
```

**التخزين:** `tharwa-benchmark_v1` (Supabase `user_settings` + cache محلي)

---

## 15. مهام المحفظة

**الملف:** `tasks.html` / `js/tasks.js`

### أنواع المهام

| النوع | الرمز | الوصف |
|---|---|---|
| `liquidation` | 🔴 | تصفية كاملة |
| `reduction` | ⚖️ | تخفيف نسبة |
| `monitoring` | 👁 | تحت المراقبة |
| `accumulation` | 🟢 | تجميع / إضافة |
| `hold` | 🔵 | احتفاظ |

### دورة الحياة

```
active → done (إغلاق مع تسجيل closed_at)
active → cancelled
```

لا حذف حقيقي — soft delete فقط.

> **ملاحظة معمارية:** توليد المهام التلقائي من انحرافات أهداف المحفظة لم يعد موجوداً في `tasks.js` — انتقل منطق اقتراح الإجراء إلى `decision-engine.js` (قسم 29)، الذي يقرأ حقول خطة الأسعار (`accumulate_at`/`trim_from`/`trim_to`/`liquidate_above`) من `portfolio_tasks` كمُدخل بدل توليدها. صفحة المهام الآن فلترة/إدارة يدوية بحتة.

---

## 16. دفتر المراجعة

**الملف:** `review-log.html` / `js/review-log.js`

### المدخلات

| الحقل | الوصف |
|---|---|
| الرمز + الاسم + القطاع | — |
| تاريخ المراجعة | — |
| الملاحظات | نص حر |
| المرفقات | txt / md / xlsx / csv |

### القيود

- حد أقصى 2MB لكل إدخال (إجمالي المرفقات)
- حد أقصى 10 مرفقات لكل إدخال
- المحتوى محفوظ في Supabase كـ base64
- `batch=5` في الباكب (حفاظاً على حجم الطلب)

---

## 17. صافي الثروة

**الملف:** `networth.html` / `js/networth.js`

### مكونات الحساب

```
أسهم (تلقائي)      = Σ(shares × current_price)
عقارات (تلقائي)    = Σ(current_value) [غير المباعة]
أصول يدوية         = Σ(value) من nw_assets
─────────────────────────────────────────
إجمالي الأصول      = أسهم + عقارات + أصول
الالتزامات         = Σ(value) من nw_liabilities
─────────────────────────────────────────
صافي الثروة        = إجمالي الأصول − الالتزامات
```

### فئات الأصول اليدوية

حساب بنكي/نقدي | صكوك/سندات | مركبة | أخرى

### فئات الالتزامات

بطاقة ائتمان | قرض | رهن عقاري | أخرى

### اللقطات (Snapshots)

- إضافة يدوية: التاريخ + القيمة + ملاحظات
- تلقائية من dashboard.html مرة شهرياً (`notes = "auto-YYYY-MM"`)

> **AUDIT-FIX:** نافذة التأكيد استُبدلت بـ `confirmAsync()` بدل `confirm()` الأصلي (توافق جوال/CSP).

---

## 18. مقسّم الراتب

**الملف:** `salary.html` / `js/salary.js`
**التخزين:** Supabase `user_settings` (مفتاح `salary_planner_v1`) — مصدر الحقيقة، مع cache في localStorage

### الفئات الافتراضية

| الفئة | اللون | النوع |
|---|---|---|
| مصاريف | أحمر | `expense` |
| ادخار / طارئ | أخضر | `savings` |
| أصول | أزرق | `asset` |
| محفظة التقاعد | بنفسجي | `asset` |

المستخدم يمكنه إضافة فئات مخصصة بألوانه. **ترقية تلقائية للفئات القديمة بلا `type`:** مطابقة أولاً بجدول أسماء معروف (`cat_expenses`→expense، `cat_retirement`→asset...)، ثم تخمين من اسم الفئة (كلمات مثل "ادخار/طارئ" → savings، "أصول/تقاعد/عقار/صكوك/سهم/ذهب" → asset، غير ذلك → expense).

### الحسابات

```
إجمالي الرواتب       = Σ(راتب) للفترة المختارة
إجمالي الموزَّع       = Σ Σ(مبالغ التخصيص) عبر كل الفئات
المتبقي غير الموزَّع  = إجمالي الرواتب − إجمالي الموزَّع

لكل نوع (مصاريف/ادخار/أصول):
  مبلغ_النوع = Σ(مبالغ فئات هذا النوع)
  نسبة_النوع = مبلغ_النوع ÷ إجمالي_الراتب × 100

لكل فئة فردية:
  مبلغ_الفئة = Σ(مبالغ تخصيص هذه الفئة)
  نسبة_الفئة = مبلغ_الفئة ÷ إجمالي_الراتب × 100

المتبقي الشهري = راتب_الشهر − Σ(تخصيصات الشهر)   [أخضر إن ≥0، أحمر إن سالب]
```

**فلتر نطاق التاريخي:**
```
year_month = سنة×100 + شهر
من = (سنة_من×100+شهر_من) أو 0 (بلا حد أدنى) | إلى = (سنة_إلى×100+شهر_إلى) أو 999999 (بلا حد أعلى)
```

> لا توجد صيغة "50/30/20" مبرمجة فعلياً — هي توصية إرشادية نصية فقط في تلميح (tooltip)، وليست قاعدة محسوبة.

### الرسوم البيانية (4 رسوم)

1. **الراتب مقابل التوزيع عبر الزمن** (مكدس / خط / جدول): كل فئة كخط/عمود شهري + خط الراتب فوقها
2. **توزيع الفئات** (donut / bars / cards): حصة كل فئة من الإجمالي الموزَّع
3. **المتبقي الشهري** (bars / line / table): لوّن حسب الإشارة (موجب أخضر، سالب أحمر)
4. **المقارنة السنوية** (bars / cards / table): إجمالي الراتب مقابل الموزَّع مقابل المتبقي لكل سنة

---

## 19. العقارات

**الملف:** `realestate.html` / `js/realestate.js`

### المدخلات

| الحقل | الوصف |
|---|---|
| الاسم + النوع | فيلا / شقة / أرض / تجاري... |
| تكلفة الشراء | ر.س |
| القيمة الحالية | ر.س (يدوي) |
| الحالة | مملوك / مؤجر / مباع |
| الإيجار الشهري | ر.س |
| تاريخ الشراء | — |

### إحصائيات KPI

```
إجمالي القيمة الحالية  = Σ(current_value) [غير المباعة]
إجمالي تكلفة الشراء   = Σ(purchase_value) [غير المباعة]
إجمالي الإيجار الشهري = Σ(monthly_rental) [المؤجرة]
مكاسب القيمة          = إجمالي الحالية − إجمالي الشراء
```

### التكامل

- العقارات تُحسب تلقائياً في networth.html و dashboard.html
- الحالة `sold` لا تُحسب في صافي الثروة (ولا في `fireBase` — العقار أصل غير سائل)

---

## 20. الصكوك

**الملف:** `sukuk.html` / `js/sukuk.js`
**التخزين:** Supabase `user_settings` (مفتاح `sukuk_planner_v1`) + cache محلي

### الحالات

| الحالة | اللون |
|---|---|
| مشترك | أخضر |
| مغلق | رمادي |
| متعثر | أحمر |
| مخطط له | أزرق |

### الحسابات

```
العائد الإجمالي % = (العائد السنوي ÷ 100) × (المدة ÷ 12)
الإجمالي المستلم  = المبلغ × (1 + العائد الإجمالي ÷ 100)
صافي الربح        = الإجمالي − المبلغ
```

### التكامل

إجمالي الصكوك النشطة يظهر في بطاقة تخصيص الأصول بالداشبورد وضمن `fireBase` لهدف FIRE

---

## 21. أهداف الحياة

**الملف:** `life-goals.html` / `js/life-goals.js`
**التخزين:** `life_goals_v1` في localStorage

### المدخلات

| الحقل | الخيارات |
|---|---|
| المجال | شخصي / عائلي / مالي / صحي / تعليمي / ديني / مهني / ترفيهي |
| الحالة | قيد التنفيذ / مكتمل / مؤجل / ملغي |
| التقدم % | 0–100 (شريط بصري) |
| الأولوية | 1 عالية / 2 / 3 منخفضة |
| تاريخ البدء / المستهدف | — |

### إحصائيات

```
إجمالي / نشطة / مكتملة / مؤجلة / ملغاة
متوسط التقدم % = Σ(progress) ÷ عدد الأهداف
```

---

## 22. مخزون المنزل

**الملف:** `inventory.html` / `js/inventory.js`
**التخزين:** `inventory_v1` في localStorage

### المدخلات

| الحقل | الخيارات |
|---|---|
| الفئة | نص حر (أجهزة / أثاث / ملابس...) |
| الموقع | نص حر (غرفة نوم / مطبخ...) |
| الحالة | جيد / مستعمل / متضرر / للاستبدال / مفقود |
| العدد | — |
| السعر التقريبي | — |

### الإحصائيات

إجمالي العناصر + توزيع الحالات + تحذيرات "للاستبدال" و"مفقود"

---

## 23. المتابعة المدرسية

**الملف:** `school.html` / `js/school.js`
**التخزين:** Supabase `user_settings` (مفتاح `school_tracker_v2`) + cache محلي — يدعم أبناء متعددين

### 3 تبويبات لكل طفل: أهداف الحياة / أهداف الدراسة / الدرجات

**بنية هدف واحد (حياة أو دراسة):** `{id, desc, cat, priority(1–4), status, progress(0–100%), year, amount, notes}`

### حسابات الدرجات (كاملة)

```
معدل المادة الواحدة (موزون بأوزان الفصول):
  wScore = Σ(درجة_الفصل × وزن_الفصل)  لكل فصل له درجة مُدخلة
  wSum   = Σ(وزن_الفصل)               لكل فصل له درجة
  متوسط_المادة = wScore ÷ wSum   (فارغ إن لا توجد درجات)
  نسبة_المادة% = متوسط_المادة ÷ الحد_الأقصى_للمادة × 100

  التقدير اللوني: فارغ=رمادي | ≥90%=A (أزرق داكن) | ≥75%=B (أزرق) | ≥60%=C (أصفر) | <60%=D (أحمر)

معدل الفصل الواحد (بسيط عبر كل المواد):
  لكل مادة لها درجة في هذا الفصل: (الدرجة ÷ الحد_الأقصى_للمادة) × 100
  معدل_الفصل = متوسط هذه النسب

المعدل الكلي للسنة (موزون بأوزان الفصول):
  ovScore = Σ((معدل_الفصل÷100) × وزن_الفصل)  لكل فصل له معدل
  ovSum   = Σ(وزن_الفصل)
  المعدل_الكلي% = ovScore ÷ ovSum × 100

الأوزان: اختيارية بالكامل — إن لم تُحدَّد لأي فصل تُعامَل كوزن = 1 (تساوي الفصول)
```

**رسم اتجاه التقدم:** خط عبر كل (سنة·فصل) له معدل كلي محسوب؛ نقطة واحدة فقط → رسالة "لا تروي اتجاهاً" بدل رسم مضلِّل.

### المتابعة اليومية (تبويبات جديدة)

| التبويب | الحقول | الحساب |
|---|---|---|
| **الحضور** | حالة: حاضر/غائب/متأخر/معذور | عدّاد لكل حالة (5 شارات ملخّصة) |
| **السلوك** | نوع: إيجابي/سلبي + عنوان وملاحظة | عدّاد إيجابي مقابل سلبي |
| **الواجبات** | حالة: معلّق/منجز/متأخر + تاريخ استحقاق | تحديث تلقائي: `معلّق` + تاريخ الاستحقاق < اليوم → `متأخر`؛ ترتيب حسب الأقرب استحقاقاً |
| **الاختبارات** | عنوان + مادة + تاريخ | ترتيب زمني تصاعدي؛ "القادم" = أقرب اختبار لم يمرّ بعد؛ عرض الأيام المتبقية (اليوم/غداً/بعد N يوم/قبل N يوم) |

---

## 24. متابعة كندة

**الملف:** `school-kanda.html` / `js/school-kanda.js`
**التخزين:** Supabase `user_settings` (مفتاح `school_kanda_v1`) + cache محلي

نسخة مخصصة لطالبة واحدة (كندة). نفس بنية school.html لكن مبسطة — معدل الفصول = متوسط الدرجات المملوءة.

**البنية:**
```json
{
  "profile":     { "name": "كندة", "birth": "YYYY-MM-DD" },
  "lifeGoals":   [{ "id": "...", "desc": "...", "year": 0, "status": "..." }],
  "schoolGoals": [{ "id": "...", "desc": "...", "year": 0, "status": "..." }],
  "years":       [{ "id": "...", "label": "...", "class": "...", "school": "..." }],
  "subjects":    [{ "id": "...", "name": "..." }],
  "grades":      { "yearId": { "subjectId": { "t1": 0, "t2": 0, "t3": 0 } } }
}
```

> **AUDIT-FIX:** إدخال الدرجات عبر `prompt()` استُبدل بنمط نافذة DOM آمن.

---

## 25. مزامنة المعاملات

**الملف:** `reconcile.html` / `js/reconcile.js`

### الوظيفة

استيراد كشف حساب الوسيط (Excel) ومقارنته مع معاملات التطبيق، بمطابقة تلقائية حسب التاريخ والكمية والسعر.

### المدخلات

- ملف Excel من منصة تداول
- الأعمدة: اسم السهم + الكمية + التاريخ

### المعالجة

```
BROKER_NAME_MAP: أسماء الوسيط → رموز التطبيق
مثال: 'SAUDI ARAMCO' → '2222'
       'STC'          → '7010'
```

### المخرجات

- عرض معاملات الوسيط
- مقارنة مع معاملات التطبيق
- تمييز الفوارق والمعاملات الغائبة

---

## 26. أدوات التحليل

### أ) حاسبة متوسط سعر الشراء

**الملف:** `avg-calculator.html` (لا JS منفصل — منطق inline)

**الوظيفة:** تحسيب متوسط التكلفة المرجّح عند الشراء على دفعات متعددة

**المدخلات:**
- الأسهم الحالية + السعر الحالي (للحساسة الأولى)
- دفعات إضافية: أسهم + سعر لكل دفعة (صفوف قابلة للإضافة)

**الحساب:**
```
new_avg = Σ(shares_i × price_i) / Σ(shares_i)
```

**المخرجات:** متوسط السعر الجديد | إجمالي الأسهم | إجمالي التكلفة | شريط توزيع تأثير كل دفعة على المتوسط

**لا تخزين** — حاسبة آنية فقط

---

### ب) تقدير القيمة العادلة (موسَّعة بالكامل — سجل دائم + 7 نماذج)

**الملف:** `stock-valuation.html` (منطق inline)
**التخزين:** Supabase `user_settings` (مفتاح `valuation_history_v1`) — **لم تعد بلا تخزين**، مع هجرة تلقائية لأي بيانات قديمة كانت في localStorage إلى Supabase عند أول تحميل

**الوظيفة:** تقدير القيمة العادلة للسهم حسب نوع الأصل (متوافقة مع منهجية CLAUDE.md في القسم 3 من الدستور)، مع سجل تاريخي لكل عملية تقييم لكل سهم.

**النماذج المدعومة:**

| النموذج | الصيغة | ملاحظة |
|---|---|---|
| DCF (تدفق نقدي مخصوم) | خصم 5 سنوات تدفقات + قيمة طرفية | شرط: `fcf>0` و `معدل_الخصم > نمو_نهائي` |
| جراهام المعدّلة | `V = EPS × min(8.5+2g, 25) × 4.4 ÷ Y` | `Y`=عائد سندات 10سنوات؛ سقف مضاعف 25 يمنع المبالغة |
| P/E النسبي | `V = EPS × min(P/E_القطاع, 40)` | سقف 40 يمنع تضخيم التقييم |
| جوردون (DDM) | `V = D₁/(r−g) = Div×(1+g)/(r−g)` | يشترط `r>g` وتوزيعات > 0 |
| القيمة الدفترية المعدَّلة | `V = القيمة_الدفترية × P/B_عادل` | — |
| REIT — NAV | `(إجمالي NAV − الديون) ÷ عدد الوحدات` | القياس الصحيح للريتات (وليس EPS) |
| REIT — P/FFO / Cap Rate | `FFO × مضاعف` أو `الدخل الصافي ÷ قيمة الأصول` | — |
| بنوك (وضع مخصص) | `P/B: BVPS×P/B_عادل` أو `P/E: EPS×P/E_عادل` | مع فلاتر أمان: CET1, NPL, تغطية المخصصات, LDR |

**WACC (لنموذج DCF):**
```
WACC = (E/V × Re) + (D/V × Rd × (1−T))
Re = Rf + β × (Rm − Rf)
```

**ميزات إضافية:**
- كل حقل مدخلات مصحوب بوسم وحدة (ر.س / % / ×)
- حقل "تقييم Perplexity" (لصق تحليل خارجي) يُحفظ مع كل عملية
- حقل ملاحظات شخصية منفصل
- تعديل عملية سابقة من السجل + حفظ التحديث
- بحث فوري في السجل بالرمز أو الاسم
- حذف بتأكيد `confirmAsync()`
- تصدير السجل كاملاً كـ Markdown
- إعادة حساب كل السجل بالمعادلات المحدَّثة (زر واحد)
- تحليل اتجاه ذكي: مقارنة عمليات نفس الرمز عبر الزمن مع تنبيهات (مثال: تحذير عند ارتفاع القيمة العادلة بدون تحسّن أرقام — يخدم قاعدة التثبيت في CLAUDE.md §4)

---

### ج) تقييم أمان المحفظة (صيغة دقيقة 0–10)

**الملف:** `portfolio-rating.html` (منطق inline)
**التخزين:** لقطة في `portfolio_rating_snapshot_v1` (Supabase `user_settings`)، debounce ثانية واحدة

**نظام النقاط (أساس 10 نقاط + خصومات):**

| البند | النقاط القصوى | التدرّج |
|---|---|---|
| التنويع الفعلي | 2.5 | وحدات فعّالة = أسهم + (ETF×30) + (ريت×5) + (صكوك×3) |
| التنوع القطاعي | 1.5 | قطاع واحد=0 · 2–4=0.6 · 5–7=1.1 · 8+=1.5 |
| أفق الاستثمار | 1.0 | قصير(3سنوات)=0.3 · متوسط=0.65 · طويل=1.0 |
| تركيز أكبر سهم | 1.0 | <15%=كامل · ≤20%=0.75 · ≤30%=0.4 · >30%=0 |
| تركيز أكبر قطاع | 1.0 | <30%=كامل · ≤45%=0.6 · ≤60%=0.3 · >60%=0 |
| فئات الأصول | 1.0 | 3+فئات=1.0 · فئتان=0.6 · فئة=0.3 |
| التوزيع الجغرافي | 1.0 | محلي فقط=0.55 · إقليمي=0.70 · دولي=0.85 · متنوع=1.0 |
| احتياطي الكاش | 0.5 | نعم=0.5 · لا=0 |
| أصول تحوّط (ذهب) | 0.5 | منخفض=0.15 · متوسط=0.35 · عالٍ=0.5 |

**الخصومات:**
```
سهم واحد >40%: −2.5   | سهم 30–40%: −1.2   | سهم 20–30%: −0.4
قطاع >70%:     −2.0   | قطاع 50–70%: −0.8
تناقض (تنويع عالٍ + قطاع مهيمن): −0.5
رافعة مالية: −0.75 إلى −3.5 حسب النسبة
```

**المخرج:** درجة إجمالية 0–10 + ملاحظات نصية لكل محور

---

### د) حاسبة صندوق الطوارئ

**الملف:** `emergency-fund.html` (منطق inline)

**الوظيفة:** يحسب حجم صندوق الطوارئ المُوصى به

**المدخلات:**
- المصاريف الشهرية (السكن + الفواتير + المعيشة + النقل + الالتزامات + أخرى)
- عدد أشهر التغطية المطلوبة (3 / 6 / 9 / 12)
- المبلغ المدَّخر حالياً (ر.س)

**الحساب:**
```
الهدف    = مصاريف_شهرية × عدد_الأشهر
الفجوة   = الهدف − المدَّخر_حالياً
نسبة الإنجاز = المدَّخر ÷ الهدف × 100
```

**مستويات الأمان:** 3 أشهر (موظف مستقر) · 6 (عمل حر، افتراضي) · 9 (دخل غير منتظم) · 12 (صاحب عمل)

**نصائح ذكية:** تحذيرات إذا السكن > 40% من المصاريف أو الالتزامات > 30%، مع تقدير الوقت اللازم للوصول للهدف

**لا تخزين** — حاسبة آنية فقط

---

### هـ) نصائح المستثمر

**الملف:** `invest-tips.html`

محتوى ثابت — مجموعة نصائح استثمارية مكتوبة بالعربية. لا تخزين ولا حسابات.

---

## 27. الإعدادات

**الملف:** `settings.html` / `js/settings.js`

### أ) عتبات ألوان التنبيهات

**المدخلات:** حد الأخضر % (افتراضي: 1) / حد الأصفر % (افتراضي: 3) — يجب أخضر < أصفر

**التأثير:** يُستخدم في targets.html، watchlist.html، decision-engine.html، وبانر الداشبورد

### ب) تصدير النسخة الاحتياطية

**المخرج:** ملف JSON يشمل **17 جدولاً** في Supabase + **18 مفتاحاً** في localStorage

**جداول Supabase الكاملة (TABLES):**
```
holdings · transactions · dividends · cashflow_entries · net_worth_snapshots ·
nw_assets · nw_liabilities · real_estate · user_stocks · stock_targets ·
sector_targets · watchlist · portfolio_cash · portfolio_tasks ·
review_log · review_log_attachments · user_settings
```

> `user_settings` (الجدول السابع عشر — جديد) هو مصدر الحقيقة لبيانات الراتب والصكوك وهدف التقاعد والمتابعة المدرسية ومؤشر تاسي وسجل التقييم ولقطات محرك القرار — **لازم يُنسخ ويُستعاد** لأن localStorage مجرد cache له.

**حجم الـ batch:**

| الجدول | الـ batch |
|---|---|
| `transactions` | 50 |
| `holdings` | 200 |
| `review_log_attachments` | 5 |
| البقية | 500 |

**ملاحظة:** المفاتيح تُقرأ بـ `userLsKey(k)` لعزل بيانات كل مستخدم.

### ج) استعادة من نسخة احتياطية

**الخطوات:**
1. Dry Run: تحقق من صحة الملف (الإصدار، الجداول، الحقول الأساسية)
2. عرض ملخص + تأكيد مزدوج
3. حفظ نسخة طارئة محلية في `tharwa_emergency_backup` (localStorage فقط، غير مُزامَنة)
4. حذف البيانات الحالية بترتيب عكسي عن TABLES (FK children أولاً: `review_log_attachments` → بقية الجداول)
5. إدراج البيانات (بنفس ترتيب TABLES) مع الحفاظ التام على IDs (لضمان صحة `entry_id` في المرفقات)
6. استعادة localStorage
7. redirect للداشبورد

### د) استعادة النسخة الطارئة

إذا فشلت الاستعادة: نسخة طارئة محفوظة مؤقتاً في `tharwa_emergency_backup`

### هـ) تقرير المراجعة الشهرية (Markdown) — 29 قسماً

ملف `.md` شامل يُصدَّر لقراءة الذكاء الاصطناعي. الأقسام:

```
1. الأسهم الحالية              11. هدف FIRE
2. سجل المعاملات (+ فحص VAT)   12. الأداء التفصيلي لكل سهم
3. الأرباح الموزعة              13. ملخص شهري للأرباح
4. التدفقات النقدية             14. مخطط الراتب والتوزيعات
5. صافي الثروة                  15. الصكوك والسندات
6. العقارات                     16. الأهداف الحياتية
7. أهداف الأوزان                17. قاعدة بيانات الأسهم
8. قائمة المراقبة                18. دفتر المراجعة
9. المهام                       19. المؤشر المرجعي (تاسي)
10. الملخص الإحصائي (XIRR, Forward YOC, TTM)   20. مخزون المنزل
21. المتابعة المدرسية            26. محرّك القرار (جديد)
22. متابعة كندة الخاصة           27. تقييم أمان المحفظة (جديد)
23. سجل حاسبة القيمة العادلة     28. مواعيد آخر تحديث الأسعار (جديد)
24. مرفقات دفتر المراجعة         29. الأداء المعدَّل بالزمن TWR (جديد)
25. إعدادات التطبيق
```

**فحص اتساق VAT (جديد، قسم 2):** يحسب `VAT_متوقع = العمولة × 0.15` ويقارنه بالمخزَّن فعلياً، ويُظهر أي معاملة غير متطابقة (عرض فقط، بدون تعديل تلقائي).

**قسم 26 — محرّك القرار:** آخر لقطة محفوظة (`decision_engine_snapshot_v1`) + جدول القرار الكامل لكل سهم مع الأولويات والأسباب + الثوابت الدستورية المستخدَمة + المدخلات اليدوية المحفوظة لكل سهم.

**قسم 29 — الأداء المعدَّل بالزمن:** حساب TWR بطريقة Modified Dietz (نفس منهج قسم 14) + عوائد شهرية + مقارنة بتاسي وAlpha.

### و) تصفير البيانات

- تأكيد مزدوج
- يمسح كل الجداول + localStorage
- الحساب يبقى

### ز) حذف الحساب

- يطلب إدخال البريد الإلكتروني للتأكيد
- يمسح البيانات ثم `delete_own_account()` (Supabase RPC)
- تسجيل خروج تلقائي

---

## 28. لوحة الإدارة

**الملف:** `admin.html` / `js/admin.js`
**الوصول:** `app_metadata.roles` تتضمن `'admin'` — محمي بفحص مزدوج (JWT + رسم DOM يدوياً)

> **AUDIT-FIX (أمان حرِج):** فحص صلاحية المدير كان يعتمد على `user_metadata.is_admin` — حقل قابل للتعديل من طرف العميل عبر واجهات Supabase العامة نظرياً. تم نقله إلى `app_metadata.roles` الذي لا يُكتب إلا من الخادم (service role)، ما يمنع أي مستخدم من ترقية نفسه لمدير.

### الوحدات الست

#### 1. الهوية والتحقق (`identity`)

**البيانات:** `user_profiles`

- جدول كل المستخدمين مع: البريد | تاريخ الإنشاء | آخر ظهور | الحالة
- إجراءات: تعليق / حظر / استعادة
- فلتر بالبريد + الحالة
- مؤشرات: إجمالي المستخدمين | المفعّلون | الموقوفون | نسبة التفعيل

#### 2. الامتثال والخصوصية (`compliance`)

**البيانات:** `consent_logs`, `data_erasure_requests`

- سجل موافقات المستخدمين (GDPR-style)
- طلبات مسح البيانات: عرض + تنفيذ حذف كلي `executeErasure()`

> **AUDIT-FIX (امتثال GDPR):** تنفيذ المسح كان يحذف من 8 جداول فقط — انتهاك جزئي لالتزام "الحق في المحو". الآن يحذف من **16 جدولاً كاملة** (كل جداول قسم 3-أ عدا `admin_audit_logs` غير القابل للتعديل)، شاملاً `user_settings`، ويُسجَّل في `admin_audit_logs`.

#### 3. المؤشرات التشغيلية (`analytics`)

**البيانات:** `user_profiles` + جميع الجداول

- DAU / WAU / MAU (من `last_seen`)
- حجم كل جدول (عدد الصفوف)
- متوسط الأسهم لكل مستخدم | متوسط المعاملات | نسبة مستخدمي العقارات

#### 4. الدعم والاتصالات (`support`)

**البيانات:** `support_tickets`, `admin_broadcasts`

- قائمة تذاكر الدعم مع تفاصيل المتصفح
- إغلاق التذكرة + تسجيل في الأودت
- إرسال تنبيه جماعي: الموضوع + النص + الجمهور المستهدف → يُحفظ في `admin_broadcasts`

#### 5. الأمن وسجلات التدقيق (`security`)

**البيانات:** `admin_audit_logs`, `failed_login_attempts`, `blocked_ips`

- آخر 200 إجراء إداري
- محاولات الدخول الفاشلة مع عداد (أحمر عند ≥ 5)
- حظر IP: `blocked_ips` + تسجيل إجراء

#### 6. الصيانة والزيارات الأخيرة (`maintenance`)

**البيانات:** `site_config`, `user_profiles`

- تشغيل/إيقاف وضع الصيانة (`maintenance_mode = 'true'/'false'`)
- رسالة الصيانة المخصصة
- قائمة آخر 20 زيارة (من `last_seen`)

### دالة تسجيل الإجراءات

```javascript
logAdminAction(action_type, target_user_id, action_details)
// يُدرج في admin_audit_logs تلقائياً عند كل إجراء
// أنواع: SUSPEND_USER | BAN_USER | RESTORE_USER |
//        EXECUTE_ERASURE | BLOCK_IP | RESOLVE_TICKET |
//        SEND_BROADCAST
```

---

## 29. محرّك القرار

**الملف:** `decision-engine.html` / `js/decision-engine.js` (78 KB — صفحة جديدة بالكامل)

### الغرض

يطبّق دستور المحفظة (`CLAUDE.md`) تلقائياً على البيانات الحيّة. يقرأ الأوزان الفعلية والقيم العادلة وبيانات الاستدامة والأسعار الحالية، ثم يوصي بإجراء واحد لكل سهم:

| الإجراء | الرمز | الشرط |
|---|---|---|
| تصفية كاملة | 🔴 | فشل بوابة الاستدامة أو trigger ثابت |
| تخفيف | 🟠 | كسر سقف وزن، أو سعر في منطقة تصفية من المهام، أو انحراف أحمر |
| تجميع | 🟢 | سعر في منطقة شراء + استدامة سليمة + وزن تحت الهدف |
| مراقبة | 🟡 | تنبيه مؤقت لا يصل لتصفية (ربع ضعيف، توزيع متأجل) |
| احتفاظ | ⚪ | ضمن الحدود الخضراء |

**مبدأ التصميم:** لا اجتهاد — تطبيق قواعد ثابتة فقط. القاعدة ما تنطبق → احتفاظ.

### مصادر البيانات

**من Supabase:** `holdings` (الأوزان والأسعار)، `stock_targets` (الأهداف الفردية)، `portfolio_tasks` (خطة الأسعار: `accumulate_at`/`trim_from`/`trim_to`/`liquidate_above`)، `dividends` + `transactions` (لاشتقاق DPS واتجاه التوزيع الفعلي).

**من `user_settings` (localStorage cache):**
- `decision_engine_v1` — مدخلات المحرك اليدوية (استدامة، نوع أصل، قيادي/عادي، ملاحظات) لكل رمز
- `valuation_history_v1` — آخر تقييمين لكل سهم من حاسبة القيمة العادلة (لتطبيق قاعدة التثبيت)
- `decision_engine_snapshot_v1` — لقطة كاملة لمخرجات المحرك (تُقرأ في تقرير settings.js)
- `tharwa-alert-green`/`tharwa-alert-yellow` — عتبات انحراف الوزن

### الثوابت الدستورية (مطابقة لـ CLAUDE.md حرفياً)

```javascript
const CAPS = { single: 7, blueChip: 12, sector: 25 };  // %
const CAP_BUFFER    = 0.75;   // منطقة سماح للسهم (السقف الفعلي = CAPS + 0.75%)
const SECTOR_BUFFER = 1.25;   // منطقة سماح للقطاع (25% + 1.25%)
const PORTFOLIO_SIZE = { min: 18, max: 25 };
const FIXED_TRIGGERS = [
  { ticker: '2222', name: 'أرامكو', kind: 'reduce', price: 29, toWeight: 12 },
  { ticker: '4002', name: 'المواساة', kind: 'sell', price: 85 },
];
```

### الفلتر 1 — بوابة الاستدامة (3 محاور، كشف تلقائي + تجاوز يدوي)

```
محور التغطية:       covered ✅ | weak 🟡 (مراقبة) | uncovered 🔴 (تصفية)
محور الأساسيات:     healthy ✅ | soft 🟡 | deteriorating 🔴
محور إشارة التوزيع:  stable/growing ✅ | temp 🟡 | cut/stopped 🔴
```

**كشف اتجاه التوزيع تلقائياً (dividendTrendOf):**
```
1. DPS لكل توزيعة = المبلغ ÷ الأسهم المملوكة وقتها (من transactions)
2. توقّف تام: آخر توزيعة منذ >18 شهراً → 'stopped' (تصفية)
3. مقارنة سنوية (سنوات تقويمية كاملة فقط، تستبعد الجارية):
   التغيّر% = (DPS_آخر_سنة − DPS_سنة_سابقة) ÷ DPS_سنة_سابقة × 100
   ≤ −25%  → 'cut' 🔴
   ≥ +5%   → 'growing' ✅
   غير ذلك → 'stable' ➡️
```

**معيار التغطية حسب نوع الأصل (AUDIT-FIX 2026-07 — مطابقة CLAUDE.md §3):**
```
إسمنت/بتروكيماويات: coverBase = FCF (إن FCF > 0)
REIT:                coverBase = FFO
البقية:              coverBase = EPS
payout   = التوزيع ÷ coverBase
coverage = payout ≤ 1.0 ? 'covered' : 'weak'
```
> **قبل الإصلاح:** كان كل الأصول عدا الريت يُقاسان بـ EPS، ما يخالف دستور القسم 3 الذي يفرض FCF للإسمنت/البتروكيماويات.

**النتيجة النهائية:**
```
أي محور أحمر → 'fail' → 🔴 تصفية (ما لم يضعه المستخدم "مراقبة" يدوياً في المهام)
أي محور أصفر (بلا أحمر) → 'watch' → 🟡 مراقبة (لا تصفية آلية)
كل المحاور خضراء → 'pass' ✅
بيانات ناقصة → 'unknown' ⚪ (تُعلن صراحة، لا تُقدَّر بصمت)
```

### الفلتر 2 — قاعدة التثبيت (Stabilization Rule)

يمنع رفع القيمة العادلة بدون دليل أرقام (مطابق لـ CLAUDE.md §4):

```javascript
// قارن التقييم الحالي بآخر تقييم سابق من valuation_history_v1
if (cur.fair.avg > prev.fair.avg × 1.01) {   // ارتفعت بأكثر من 1%
  earnUp = cur.eps > prev.eps ; divUp = cur.dividends > prev.dividends ; fcfUp = cur.fcf > prev.fcf
  إن (!earnUp && !divUp && !fcfUp):
    🚩 تحذير: "القيمة العادلة ارتفعت من X إلى Y بدون دليل ارتفاع EPS/FCF/التوزيع — راجع قاعدة التثبيت"
}
```

### معالجة الأرقام العربية-الهندية في مدخلات التقييم (AUDIT-FIX 2026-07)

```javascript
parseFairValueRange(str):
  تحويل الأرقام العربية-الهندية ٠-٩ → 0-9، والفاصلة العشرية ٫ → .، وحذف فواصل الآلاف ,/،
  استخراج كل الأرقام من النص، حساب المتوسط والحد الأدنى/الأقصى
```

### الفلتر 3 — خطة الأسعار (من portfolio_tasks)

```
السعر > liquidate_above                → 🔴 تصفية (أولوية 1 — سعر التضخّم)
trim_from ≤ السعر ≤ trim_to             → 🟠 تخفيف (أولوية 2)
السعر ≤ accumulate_at + استدامة سليمة
  + الوزن < الهدف                       → 🟢 تجميع (أولوية 3)
```

**فلتر 3 دستورياً (AUDIT-FIX 2026-07) — تنبيه السعر فوق القيمة العادلة:**
```javascript
fvMargin = (القيمة_العادلة − السعر) ÷ القيمة_العادلة × 100
إن fvMargin ≤ −15% (السعر أعلى بـ15%+ من العادلة) وليست القيمة قديمة (valStale=false):
  → action='monitor', label='مرشّح تخفيف (فوق العادلة)', priority=2.7
```
> يُطبّق القسم 4 من CLAUDE.md حرفياً: "لو السعر تجاوز القيمة العادلة بهامش معتبر → مرشّح للتخفيف" — حتى لو لم تُوضع خطة أسعار يدوية بعد.

### الفلتر 4 — سقف الوزن الدستوري

```
cap = القيادي؟ 12% : 7%
weight = (shares × current_price) ÷ إجمالي_المحفظة × 100
overCap = weight > cap + CAP_BUFFER   (كسر صلب، مستقل عن القيمة العادلة)

الانحراف عن الهدف المسجَّل (إن وُجد target_weight):
  dev = weight − target_weight
  |dev| ≤ عتبة_الأخضر → أخضر | ≤ عتبة_الأصفر → أصفر (مراقبة) | > عتبة_الأصفر → أحمر (إجراء)
```

### سقف القطاع

```
sectorWeight = Σ(قيم أسهم القطاع) ÷ إجمالي_المحفظة × 100
sectorWeight > CAPS.sector + SECTOR_BUFFER (25%+1.25%=26.25%) → ⚠️ تركيز قطاعي
```

### ترتيب الأولويات

```
0   Triggers الثابتة (فوق كل شيء)
1   فشل الاستدامة / سعر تصفية من خطة الأسعار
1.5 مراقبة (استدامة ضعيفة أو قرار المستخدم)
2   كسر سقف وزن / انحراف أحمر / تخفيف من خطة الأسعار
2.5 انحراف أصفر
2.7 فوق القيمة العادلة بـ15%+ (مرشّح تخفيف، وليس إلزاماً)
3   تجميع (خطة أسعار + استدامة سليمة + وزن أقل من الهدف)
4   مرشّح تجميع (منطقة سعر جذابة، استدامة سليمة، بلا هدف مسجَّل)
5   مراقبة بقرار المستخدم
9   احتفاظ (الحالة الافتراضية)
```

### XIRR الفردي للمركز

```javascript
positionXIRR(transactions, dividends, currentMarketValue):
  flows = [كل شراء (−) ، كل بيع (+) ، كل توزيع (+) ، القيمة السوقية الحالية اليوم (+)]
  return computeXIRR(flows)   // نفس الدالة العامة في utils.js
```

### أقسام الواجهة

1. **بطاقة الملخص (Hero):** عدد الأسهم التي تحتاج قراراً
2. **شريط الملخص:** عدد كل إجراء (تصفية/تخفيف/مراقبة/تجميع/احتفاظ)
3. **نسبة تغطية الدخل:** دخل التوزيعات (TTM) ÷ الهدف الشهري 5,000 ر.س (من دستور CLAUDE.md §1)
4. **مجموعات الإجراءات:** يحتاج تصرّف 🔴 / مراقبة 🟡 / تجميع 🟢
5. **فحص سقف القطاع:** قائمة القطاعات الخارجة عن الحد
6. **بطاقات تفصيلية لكل سهم** (مطويّة افتراضياً) — الفلاتر بالترتيب مع أيقونة حالة كل واحد، المالية التفصيلية، سجل التوزيعات، مدخلات آخر تقييم، والمدخلات اليدوية المحفوظة

### بطاقة إدخال المحرك (يدوية لكل سهم)

نوع الأصل (تجاوز) · قيادي/عادي · بوابة الاستدامة الثلاثية (مع عرض الكشف الآلي وهل الإدخال اليدوي يتجاوزه) · خطة الأسعار (قراءة فقط من المهام) · آخر تقييم وحالة قِدمه · ملاحظات دستورية خاصة (مثال: 5110/إسمنت دوري) · حقل ملاحظات حر.

**الحفظ:**
```javascript
saveUserSetting('decision_engine_v1', {
  [ticker]: { assetType, blueChip, divCoverage, fundamentals, divSignal, notes }
});
saveUserSetting('decision_engine_snapshot_v1', {
  generated_at, totalValue, thresholds, caps, portfolioSize, fixedTriggers, results:[...]
});
```

---

## 30. جني الأرباح

**الملف:** `profit-taking.html` (منطق inline — لا JS منفصل ولا تخزين)

### الغرض

أداة **تعليمية بحتة** (ليست توصية بيع/شراء) تقيس مدى مبرّرية جني جزء من الربح من سهم ارتفع سعره، عبر مؤشر واحد 0–100 يمثّل درجة "التطرّف" من حيث التقييم والتركيز والزخم والسلوك.

### المرحلة 1 — 4 أسئلة سلوكية (نقاط كيفية)

| السؤال | أعلى نقاط | أدنى نقاط | أعلام خاصة |
|---|---|---|---|
| هل تجاوز السعر الهدف؟ | +3 | −3 | — |
| مدعوم بأرباح أم ضجيج؟ | +4 | −4 | `euphoria` عند اختيار "يطير بالمضاربة" |
| هل صار نسبة مخيفة من المحفظة؟ | +4 | −1 | `concentrated` عند اختيار "30–40% من المحفظة" |
| شعورك لو نزل 20% غداً؟ | +3 | −3 | — |

```
QUAL_MIN = −11  (−3−4−1−3) | QUAL_MAX = 14 (3+4+4+3)
qualRaw  = مجموع نقاط الإجابات المختارة
```

### المرحلة 2 — 4 إشارات كمية (كل واحدة تُطبَّع 0..100، والناقص يُستبعد بإعادة توزيع الأوزان)

```
1) إشارة سلوكية:  qualSig = clamp((qualRaw − QUAL_MIN) ÷ (QUAL_MAX − QUAL_MIN) × 100, 0, 100)
                  الوزن الأساسي W.qual = 0.30

2) إشارة الربح:   profitPct = (السعر − التكلفة) ÷ التكلفة × 100
                  profitSig = clamp(profitPct ÷ 150 × 100, 0, 100)   [0%→0 ، 150%+→100]
                  الوزن الأساسي W.profit = 0.15

3) إشارة الامتداد عن MA200 (اختياري):
                  extPct = (السعر − MA200) ÷ MA200 × 100
                  extSig = clamp(extPct ÷ 50 × 100, 0, 100)          [0%→0 ، 50%+→100]
                  الوزن الأساسي W.ext = 0.25

4) إشارة علاوة التقييم P/E (اختياري):
                  peRatio = P/E_السهم ÷ P/E_القطاع
                  valSig  = clamp((peRatio − 1) ÷ 1.5 × 100, 0, 100)  [1.0→0 ، 2.5+→100]
                  الوزن الأساسي W.val = 0.30
```

**تطبيع الأوزان عند غياب حقل (مثال: بلا MA200):**
```
wSum = Σ(W_k) للإشارات الموجودة فقط
usedWeight_k = W_k ÷ wSum
index = Σ(الإشارة_k × usedWeight_k)   → محدود 0..100
```

### التجاوزات (Overrides)

```
تركيز مفرط (concentrated) + ربح>40%  → 🟠 "تقليم إلزامي لإعادة التوازن"
نشوة سعرية (euphoria) + ربح>30%     → 🟡 "تحذير: نشوة سعرية — أمِّن جزءاً محافظاً"
```

### نطاقات الحكم

```
index ≥ 70:  💰 منطقة جني أرباح قوية — جنِّ 50–100% من الربح
index ≥ 45:  ✂️ تقليم جزئي — بِع 20–30% واترك الباقي
index ≥ 25:  🛡️ احتفظ ومتابعة — الارتفاع مبرَّر نسبياً
غير ذلك:     🚀 احتفاظ صلب — لا مبرّر للبيع
```

### حاسبات تعليمية مدمجة

```
القيمة العادلة من EPS:        FairValue = EPS × P/E_معقول
مقارنة نمو السعر بنمو الأرباح: ratio = %نمو_EPS ÷ %ارتفاع_السعر
                              ratio≥0.7 → نمو حقيقي صحي | ratio<0.4 → مضاربة بحتة
```

### ملخص الفروقات مع محرّك القرار

| المعيار | محرّك القرار (29) | جني الأرباح (30) |
|---|---|---|
| الغرض | قرار آلي على كل أسهم المحفظة | تقييم سلوكي لسهم واحد |
| المصادر | Supabase حيّة | يدوي بالكامل |
| الحفظ | نعم (`decision_engine_v1`) | لا (عرض فقط) |
| المؤشر | action (خروج/تخفيف/تجميع/احتفاظ) | index (0..100) |
| العتبات | ثابتة دستورياً (7%/12%/25%) | ديناميكية (أسئلة + حسابات) |

---

## 31. الخوارزميات الرئيسية

### XIRR — معدل العائد الداخلي السنوي

**الملف:** `js/utils.js` → `computeXIRR(flows)`

```
المدخلات: [{date: Date, amount: number}]
  شراء → amount سالب
  بيع / توزيعات / قيمة حالية → amount موجب

REL_TOL = Σ|amount_i| × 1e-4   (تسامح نسبي 0.01% من حجم التدفقات)

الخوارزمية:
  1. Newton-Raphson (100 تكرار، دقة 1e-7)
  2. Binary Search كـ fallback (200 تكرار، تسامح REL_TOL)
  3. النطاق: [−99.99%, +10000%]
  4. التحقق النهائي: |NPV(r)| ≤ REL_TOL

المخرج: % سنوي أو null إذا تعذّر الحساب
```

### مقياس التنويع (computeDiversification) — HHI والعدد الفعّال

**الملف:** `js/utils.js` — مصدر الحقيقة الوحيد، تستدعيه dashboard.js وwatchlist.js وperformance.js

```javascript
computeDiversification(positions)  // [{value, sector}, ...]

HHI = Σ(وزن_i²)     حيث وزن_i = قيمة_i ÷ إجمالي_القيمة   (0..1)
effectiveN = 1 ÷ HHI   (مرجع Evans & Archer 1968: ~15 سهماً ≈ 90% من منفعة التنويع)

secHHI = Σ(وزن_القطاع_j²)   (نفس الصيغة على مستوى القطاع)
N_فعّال_قطاعات = 1 ÷ secHHI

// موضع المؤشر (gaugePos) عبر استيفاء خطي بين نقاط ثابتة:
نقاط التحويل (HHI خام → gaugePos%):
  [1.000→0], [0.500→8], [0.250→22], [0.150→40],
  [0.100→62], [0.067→80], [0.050→88], [0.033→93], [0.020→97], [0.000→100]

معامل_القطاعات = min(1.0, 0.70 + 0.30 × min(1, N_فعّال_قطاعات ÷ 6))
gaugePos = round(min(97, max(3, stockGauge × معامل_القطاعات)))

المناطق: <22% مركَّز جداً 🔴 | 22–40% تركيز ملحوظ 🟠 | 40–60% تنوع معقول 🟡
         | 60–80% تنوع جيد 🟢 | ≥80% تنوع ممتاز 🟢 داكن
```

**الإخراج الكامل:** `{hhi, effectiveN, top1Pct, top1Name, secMap, sectorCount, secHHI, gaugePos, zoneLabel, zoneColor}`

### WAC — المتوسط المرجّح للتكلفة

```
عند الشراء:
  new_avg = (old_shares×old_avg + buy_shares×buy_price) / total_shares

عند البيع:
  الأسهم تنقص — WAC ثابت (يُعاد اشتقاقه زمنياً في performance.js لحساب الربح المحقق بدقة)

عند المنحة:
  الأسهم تزيد — avg_price ثابت (تكلفة = صفر)
```

### Capital-Weighted Age — عمر رأس المال الفعلي

```
لكل إيداع:
  ws += amount × أشهر_منذ_الإيداع
  wb += amount

لكل سحب:
  ws *= (1 − سحب / رصيد)
  wb -= سحب

CWA = ws / wb  (بالشهور)

مثال: 10K قبل 8 أشهر + 170K قبل 4 أشهر:
  CWA = (10K×8 + 170K×4) / 180K = 4.2 شهر فعلي

يُستخدم في: dividends.js (مؤشر ثقة التوزيعات) وforecast.js (مؤشر ثقة الإسقاط)
```

### Forward Dividend Yield (موحَّد بين dashboard.js وdividends.js)

```
لكل سهم محتفظ به:
  1. الدورية من وسيط الفجوات الزمنية بين آخر التوزيعات:
     وسيط ≤105 يوم → ربع سنوي (×4) | ≤210 يوم → نصف سنوي (×2) | >210 يوم → سنوي (×1)
  2. DPS = وسيط (median) آخر (الدورية) قيمة DPS — وليس آخر قيمة فقط (يمنع تضخيم توزيعة استثنائية)
  3. Projected = DPS × الدورية × الأسهم الحالية

Forward YOC = Σ(Projected) ÷ تكلفة الحيازات × 100
```

### حساب العمولة (تداول)

```javascript
// COMMISSION_RATE محدد كثابت — راجع عقد الوسيط قبل التغيير
// Aljazira/SNB: 0.0015 (1.5‰) | Mubasher/Albilad: 0.0025 (2.5‰)
const COMMISSION_RATE = 0.0015

commission = min(shares × price × COMMISSION_RATE, 100)
vat        = commission × 0.15
total_buy  = (shares × price) + commission + vat
total_sell = (shares × price) − commission − vat
total_grant = 0
```

### buildCumulativeCapitalMap — prefix-sum للرأس المال

```javascript
// O(M) — يُبنى مرة واحدة لكل تحميل performance.js
// بدلاً من إعادة مسح جميع التدفقات لكل شهر O(N×M)
buildCumulativeCapitalMap():
  رتّب cashflow_entries زمنياً
  اجرِ prefix-sum تراكمي
  أعِد { "YYYY-MM": total_at_end_of_month }
```

### Time-Weighted Return (TWR) — Modified Dietz

**الملفان:** `performance.js` (تبويب Max Drawdown) و`settings.js` (تقرير قسم 29)

```
لكل فترة بين لقطتين متتاليتين:
  r = (القيمة_النهاية − القيمة_البداية − صافي_التدفق) ÷ (القيمة_البداية + صافي_التدفق÷2)
  factor *= (1 + r)   // مؤشر سعري تراكمي معزول عن أثر الإيداع/السحب، يبدأ من 100

الغرض: عزل أداء قرارات الاستثمار الفعلي عن تأثير توقيت الإيداعات والسحوبات (بخلاف XIRR الذي يقيس العائد الكلي شاملاً توقيت التدفقات)
```

### درجة تقييم الأسهم في إعادة التوازن (valuationScore) — targets.js

```
مصدر 1 (70%): مناطق خطة الأسعار من portfolio_tasks (accumulate_at/trim_from/liquidate_above) — استيفاء خطي
مصدر 2 (30%): هامش القيمة العادلة من valuation_history_v1 → clamp((هامش+0.10)÷0.40, 0, 1)
الدرجة = 0.7×مصدر1 + 0.3×مصدر2 (أو أحدهما منفرداً؛ 0.5 محايدة إن غابا معاً)
```

راجع [قسم 11](#11-أهداف-الأسهم-والقطاعات) للتفصيل الكامل بالأمثلة.

---

## 32. النسخ الاحتياطي والاستعادة

### تغطية الباكب — 100% من بيانات المستخدم

| المصدر | الجداول / المفاتيح | مشمول؟ |
|---|---|---|
| Supabase | **17 جدولاً** (شامل `user_settings`) | ✅ |
| localStorage | **18 مفتاحاً** (userLsKey) | ✅ |

### سلامة FK عند الاستعادة

```
ترتيب الحذف (FK children أولاً):
  review_log_attachments → ثم بقية الجداول (عكس ترتيب TABLES)

ترتيب الإدراج (TABLES):
  holdings → transactions → dividends → ... → review_log → review_log_attachments → user_settings

حفظ IDs:
  review_log.id محفوظ → يضمن صحة entry_id في attachments
```

### نتائج التحقق

```
insert: review_log قبل attachments     ✅
delete: attachments قبل review_log     ✅
FK:     entry_id = review_log.id       ✅
جداول:  17                             ✅
LS keys: 18                            ✅
```

---

## 33. utils.js

**الملف:** `js/utils.js` — مُحمَّل في كل صفحة (40 KB)

### دوال التنسيق

```javascript
formatSAR(amount, showSign=false)  // ريال سعودي — −0 يُعامَل كـ 0
formatNum(num, decimals=2)         // تنسيق رقمي
formatShares(num)                  // أسهم (بدون trailing zeros)
formatDate(dateStr)                // تاريخ عربي مختصر
todayISO()                         // YYYY-MM-DD
parseDateLocal(s)                  // "YYYY-MM-DD" → Date (local midnight — لا UTC shift)
```

### الحسابات المالية

```javascript
computeXIRR(flows)                 // [{date,amount}] → % أو null
calcCommission(shares, price)      // → {commission, vat, totalBuy, totalSell, tradeValue}
computeDiversification(positions)  // [{value, sector}] → {hhi, effectiveN, gaugePos, zoneLabel, ...}
                                    // (راجع الصيغة الكاملة في قسم 31) — مصدر حقيقة مشترك بين
                                    // dashboard.js / watchlist.js / performance.js
```

### إدارة البيانات

```javascript
supaQuery(queryFn, errorMsg)       // wrapper موحّد — يعيد null عند الخطأ + toast
saveUserSetting(key, value)        // upsert في user_settings (Supabase) — عبر الأجهزة
loadUserSetting(key)               // قراءة من user_settings مع فلتر user_id صريح
userLsKey(key)                     // → "u:{userId}:{key}" (لعزل localStorage بين المستخدمين)
```

### واجهة المستخدم

```javascript
showToast(msg, type)               // toast آمن (textContent لا innerHTML) — type: success|error|info|warning
confirmAsync(message)              // نافذة تأكيد مخصصة (بديل window.confirm للموبايل)
                                    // — استُخدمت الآن في cashflows.js، networth.js، stock-valuation.html وغيرها
showNotePopup(btnEl)               // popup ملاحظة قابل للإغلاق — بدون innerHTML
setActiveNav(linkId)               // يُعلّم الرابط النشط في nav
toggleNavGroup(id)                 // فتح/إغلاق مجموعة nav (محفوظ في nav_groups_v1)
```

### نمط شرح البطاقات (Card Info Pattern) — جديد

```javascript
window.openInfoModal(title, bodyHtml)
// نافذة معلومات مشتركة لكل الصفحات: عنوان + زر إغلاق + جسم HTML
// تُغلق بالضغط على الخلفية أو Escape

window.showCardInfo(key)
// يقرأ من قاموس window.CARD_INFO الذي تعرّفه كل صفحة بنفسها
// body قد يكون نصاً ثابتاً أو دالة تُعيد نصاً محسوباً ديناميكياً وقت الفتح (لعرض أرقام حيّة)
// الاستخدام: <button class="info-btn" onclick="showCardInfo('eps')">ⓘ</button>
// مستخدَم بكثافة في dashboard.js (بطاقات التنويع/الصحة/التعادل/FIRE...) وصفحات أخرى
```

### Inline Editing

```javascript
enableInlineEditing(tbody, onSave)
// كل td تحمل: data-table, data-id, data-field, data-type, data-raw
// نقرة → input/select يظهر
// blur/enter → يحفظ في Supabase + onSave(id, field, newVal)
// رفض NaN للحقول الرقمية + allowlist أمان (INLINE_EDIT_ALLOWLIST)
```

**INLINE_EDIT_ALLOWLIST — الحقول المسموح بتعديلها:**

```javascript
transactions:     date, ticker, name, type, shares, price, notes
holdings:         ticker, name, sector, shares, avg_price, current_price, target_weight, notes
real_estate:      current_value, status, notes, name, rent_amount, purchase_price
dividends:        amount, date, year, month, ticker, name, notes
cashflow_entries: amount, date, type, notes, description
stock_targets:    target_pct, entry_price, exit_price, notes
sector_targets:   target_pct
// ... + 5 جداول أخرى
```

### أدوات أخرى

```javascript
esc(v)                              // HTML escape لمنع XSS
uid()                               // UUID (crypto.randomUUID أو fallback)
exportCSV(filename, headers, rows)  // CSV + BOM (العربية) — revokeObjectURL مؤجل
chartDefaults()                     // إعدادات Chart.js الموحدة (ألوان + خطوط Tajawal)
toggleTheme(isLight)                // dark ↔ light (localStorage)
zoomIn() / zoomOut()                // حجم الخط (11–21 px)
```

### قاموس الأسهم السعودية (TICKER_DB)

**285 رمزاً** (تحديث كبير من ~40 سابقاً):
`'2222': 'أرامكو السعودية'` | `'1120': 'مصرف الراجحي'` | `'7010': 'الاتصالات السعودية'` ...

### القطاعات الرسمية (OFFICIAL_SECTORS)

قائمة كاملة بقطاعات السوق السعودي المعتمدة — بلا تغيير.

---

## 34. الأمان والبنية الدفاعية

### طبقات الأمان

```
1. Supabase Auth (JWT)
   └─ كل طلب يحمل access_token
   └─ انتهاء الجلسة تلقائياً

2. Row Level Security (RLS) — كل جدول مستخدم
   └─ USING (auth.uid() = user_id)
   └─ WITH CHECK (auth.uid() = user_id)
   └─ المستخدم لا يستطيع رؤية أي صف لمستخدم آخر

3. Admin Tables
   └─ (auth.jwt() -> 'app_metadata' ->> 'roles') تتضمن 'admin'
   └─ AUDIT-FIX: لم يعد يعتمد على user_metadata القابل للتعديل نظرياً من العميل

4. Client-side requireAuth()
   └─ طبقة دفاع إضافية قبل تحميل أي صفحة

5. Inline Edit Allowlist
   └─ رفض أي table/field غير موجود في INLINE_EDIT_ALLOWLIST
   └─ RLS يحمي على مستوى الصفوف (user_id)
```

### XSS Prevention

```javascript
// showToast: textContent لا innerHTML
item.querySelector('.toast-msg').textContent = msg;

// showNotePopup: createTextNode لا innerHTML
body.appendChild(document.createTextNode(line));

// esc(): يُطبَّق على كل قيمة تُدرج في innerHTML
esc(v) → &amp; &quot; &#39; &lt; &gt;
```

### إدارة نطاق localStorage

```javascript
// كل مفتاح مشفَّر بـ userId لمنع التسريب على الأجهزة المشتركة
userLsKey('salary_planner_v1') → 'u:uuid-xxx:salary_planner_v1'
```

### تدابير أخرى

- `loadUserSetting` يُضيف `eq('user_id', user.id)` صراحةً (دفاع ضد RLS misconfiguration)
- `calcCommission` يستخدم عمليات 10000× لتجنب أخطاء الفاصلة العائمة
- XIRR يتحقق من صحة الـ Date قبل الحساب + تسامح نسبي (لا مطلق)
- Edge Function CORS يقرأ `APP_ORIGIN` من env var
- يُدوَّن كل إجراء إداري في `admin_audit_logs` (immutable)
- كشوف الدخول الفاشلة مرتبة بالعداد في `failed_login_attempts`
- حذف بيانات المستخدم (تصفير/حذف حساب/تنفيذ طلب محو GDPR) يغطي الآن **16 جدولاً** بدل 8 — راجع [قسم 28](#28-لوحة-الإدارة)
- نوافذ `confirm()`/`prompt()` المتصفح الأصلية استُبدلت تدريجياً بـ `confirmAsync()` ونمط نافذة DOM آمن (توافق CSP + الجوال) في عدة صفحات: cashflows، networth، userdb، school-kanda، stock-valuation

---

## 35. Edge Function — تحديث الأسعار

**الملف:** `supabase/functions/update-prices/index.ts`
**Runtime:** Deno (Supabase Edge Functions)

### التدفق الكامل

```
1. التحقق من المستخدم:
   GET /auth/v1/user (Authorization: Bearer token)
   → 401 إذا لم يكن الرمز صالحاً

2. جلب أسهم المستخدم:
   GET /rest/v1/holdings?user_id=eq.{userId}  (SERVICE_KEY)

3. بناء رموز Yahoo: [{ticker}.SR, ...]

4. جلب cookie + crumb من Yahoo Finance:
   GET https://fc.yahoo.com → cookie
   GET /v1/test/getcrumb    → crumb (مُخفى في السجلات)

5. استعلام Yahoo Finance API:
   GET /v7/finance/quote?symbols=...

6. تصفية الأسعار:
   رفض: null | ≤ 0 | > 1,000,000 SAR

7. تحديث holdings.current_price + price_updated_at:
   PATCH /rest/v1/holdings?ticker=eq.{t}&user_id=eq.{u}  (SERVICE_KEY)

8. الاستجابة:
   { updated: N, total: M, prices: {ticker: price}, failed: [tickers] }
```

### المتغيرات البيئية المطلوبة

| المتغير | الوصف |
|---|---|
| `SUPABASE_URL` | رابط Supabase project |
| `SERVICE_ROLE_KEY` | مفتاح الخدمة (يتجاوز RLS) |
| `SUPABASE_ANON_KEY` | المفتاح العام (للتحقق من المستخدم) |
| `APP_ORIGIN` | نطاق التطبيق لـ CORS (اختياري، افتراضي `*`) |

### استدعاء من الواجهة

```javascript
// dashboard.js و watchlist.js
const { data: json } = await supabaseClient.functions.invoke('update-prices')
// json.failed[] → يُظهر toast تحذير للأسهم التي لم تُحدَّث
```

---

## 36. إضافات يوليو 2026 — تحليل المخاطر وإجراءات الشركات

خمس وظائف أُضيفت بعد مراجعة تحليلية استثمارية. كلها متوافقة مع دستور CLAUDE.md (لا زكاة، محفظة سعودية 100%).

### 36.1 مونتي كارلو ومخاطر تسلسل العوائد (forecast.js / forecast.html)

بطاقة جديدة تحت لوحة الهدف. تعالج قصور السيناريوهات الثابتة (عائد ثابت كل سنة) بمحاكاة عشوائية.

```
تجميع العوائد (Bootstrap):
  عوائد تاسي السنوية = TASI_PRICE_YE[i] ÷ TASI_PRICE_YE[i−1] − 1   (~20 عائداً 2004–2024)
  إعادة مركزة المتوسط الهندسي إلى blendedCapGrowth (نمو السيناريو المعتدل):
    k = (1 + targetGeo) ÷ (1 + geoTaريخي)   ;   r_adj = (1+r)×k − 1
    (يحفظ التقلّب وذيول الأزمات، ويضبط المتوسط فقط)

المحاكاة (N = 2000 مسار، سنوية):
  لكل سنة: r = سحب عشوائي بإحلال من التوزيع المُعاد مركزته
           value ×= (1+r) ؛ div = value×divYield ؛ if reinvest value += div ؛ value += إضافة_السنة
  القيمة النهائية الحقيقية = value ÷ (1+معدل_التضخم)^الأفق   (عند تفعيل التضخم)

المخرجات:
  نسبة النجاح = حصّة المسارات التي بلغت الهدف بقوة شراء اليوم عند سنة التقاعد
  المئينات p5/p10/p25/p50/p75/p90 لقيمة المحفظة + الدخل الشهري (p10/p50/p90)
  خطر التسلسل: الفجوة بين p10 والوسيط = هشاشة الخطة أمام انهيار قرب التقاعد
```

### 36.2 Forward DPS واعٍ للاتجاه (dividends.js `_dpsTrendAware` + dashboard.js)

يعالج تخلّف الوسيط عن الأسهم النامية (راجحي/STC) التي ترفع توزيعها بثبات.

```
window = آخر freq دفعات DPS (بالترتيب الزمني)
إن كانت تصاعدية بانتظام (كل دفعة ≥ سابقتها × 0.99) وآخرها > أولها × 1.03:
  → DPS = آخر دفعة معلنة   (السهم يرفع توزيعه بثبات)
وإلا:
  → DPS = وسيط window       (الافتراضي الآمن ضد التوزيعة الاستثنائية — كما كان)
```

### 36.3 مقاييس مُعدَّلة بالمخاطر (performance.js — بطاقات KPI جديدة)

Sharpe / Sortino / التذبذب السنوي، من سلسلة عوائد TWR (Modified Dietz) المعزولة عن التدفقات. مُعلَّمة 🟡 تقريبية (لقطات شهرية غير منتظمة).

```
عوائد الفترات: r_i = مؤشر_TWR_i ÷ مؤشر_TWR_{i−1} − 1
periodsPerYear = عدد الفترات ÷ سنوات_المدى
العائد السنوي  = (المؤشر_الأخير ÷ الأول)^(1÷سنوات) − 1   (هندسي)
التذبذب السنوي = الانحراف_المعياري(r) × √periodsPerYear
تذبذب الهبوط   = √(متوسط مربّع العوائد السالبة مقابل MAR=0) × √periodsPerYear
RISK_FREE_RATE = 3% (افتراض تخطيطي)
Sharpe  = (العائد السنوي − RF) ÷ التذبذب السنوي
Sortino = (العائد السنوي − RF) ÷ تذبذب الهبوط
```
يحتاج ≥4 لقطات (≥3 عوائد) وإلا يُعرض "بيانات غير كافية".

### 36.4 معالج تجزئة الأسهم (transactions.js — نافذة "🪓 تجزئة سهم")

يحمي سلامة WAC وXIRR من الإدخال اليدوي عند التجزئة/التجزئة العكسية.

```
forward split (10:1): factor = raw   |   reverse split (1:10): factor = 1÷raw
لكل معاملات السهم (تُقرأ كلها من قاعدة البيانات، لا الصفحة المحمّلة):
  shares ×= factor ؛ price ÷= factor   (total ثابت → WAC يتحوّل avg÷factor، XIRR بلا تغيّر)
holdings.current_price ÷= factor   ثم recomputeHoldingFromTx
التوزيعات لا تُعدَّل: مبالغها إجمالية، والأسهم-وقت-التوزيع تُشتق من المعاملات المعدَّلة.
```

### 36.5 تنبيه الارتباط بالوكالة (utils.js `computeDiversification` + بطاقة تنويع الداشبورد)

يكشف "التنويع الاسمي": HHI منخفض (يبدو متنوعاً) بينما قطاع واحد يهيمن بأسهم مترابطة.

```
topSectorPct = أكبر وزن قطاع ؛ topSectorCount = عدد أسهمه
تنبيه إذا: n ≥ 6  و  gaugePos ≥ 40  و  topSectorPct ≥ 40%  و  topSectorCount ≥ 3
الحقول المُضافة للمخرج: topSectorName, topSectorPct, topSectorCount, corrWarn, corrMsg
```
(وكيل عملي بلا سلاسل أسعار تاريخية — مصفوفة الارتباط الكاملة تحتاج تخزين أسعار غير متوفر حالياً.)

---

*— نهاية التوثيق —*
